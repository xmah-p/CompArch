- [课程概述](#课程概述)
- [计算机基本结构](#计算机基本结构)
  - [电子计算机的兴起](#电子计算机的兴起)
  - [模型机](#模型机)
  - [现代计算机](#现代计算机)
- [指令集](#指令集)
  - [x86 体系结构](#x86-体系结构)
    - [16 位时代：x86-16（IA-16）](#16-位时代x86-16ia-16)
    - [32 位时代：IA-32](#32-位时代ia-32)
    - [64 位时代：x86-64](#64-位时代x86-64)
  - [x86 指令简介](#x86-指令简介)
  - [MIPS 体系结构](#mips-体系结构)
- [算术逻辑单元](#算术逻辑单元)
  - [门电路](#门电路)
  - [寄存器](#寄存器)
  - [加法器](#加法器)
- [乘法器与除法器](#乘法器与除法器)
  - [乘法器](#乘法器)
  - [除法器](#除法器)
- [单周期处理器](#单周期处理器)
  - [数据通路](#数据通路)
  - [控制信号](#控制信号)
  - [控制信号的集成](#控制信号的集成)
- [控制器的基本原理](#控制器的基本原理)
  - [硬布线控制器](#硬布线控制器)
  - [微程序控制器](#微程序控制器)
- [流水线处理器](#流水线处理器)
  - [流水线冒险](#流水线冒险)
- [存储层次结构](#存储层次结构)
  - [DRAM](#dram)
  - [SRAM](#sram)
  - [Cache](#cache)
  - [单位](#单位)
- [中断和异常](#中断和异常)
  - [内部中断](#内部中断)
  - [外部中断](#外部中断)
- [输入输出接口](#输入输出接口)
  - [I/O 端口及其编址](#io-端口及其编址)
  - [I/O 控制方式](#io-控制方式)
    - [程序控制方式](#程序控制方式)
    - [中断控制方式](#中断控制方式)
    - [直接存储器访问（DMA）](#直接存储器访问dma)
  - [并行接口](#并行接口)
  - [并口芯片应用实例](#并口芯片应用实例)
- [总线](#总线)
  - [总线的基本结构](#总线的基本结构)
  - [总线协议](#总线协议)
  - [AHB 时序图](#ahb-时序图)
    - [读写一个数据](#读写一个数据)
    - [主模块发起传输时，从模块未准备好](#主模块发起传输时从模块未准备好)
    - [多个连续传输](#多个连续传输)
    - [大量连续数据传输](#大量连续数据传输)
- [中断控制器及定时器](#中断控制器及定时器)
  - [中断控制器](#中断控制器)
  - [定时器](#定时器)
  - [南北桥架构的演变](#南北桥架构的演变)
- [流水线优化技术](#流水线优化技术)
  - [返回地址栈](#返回地址栈)
- [计算机系统先进技术](#计算机系统先进技术)
  - [片上互连结构](#片上互连结构)
  - [内存技术](#内存技术)
  - [中断机制](#中断机制)
  - [芯片制造和封装](#芯片制造和封装)
  - [指令系统](#指令系统)
    - [并行结构](#并行结构)
    - [GPU](#gpu)
- [考试范围](#考试范围)

# 课程概述

# 计算机基本结构

## 电子计算机的兴起

**现代电子计算机之父**：冯·诺依曼

ENIAC（Electronic Numerical Integrator and Computer，电子数字积分器和计算机）：  
- 首台**通用**电子计算机
- 1946年2月14日
- 1943年开始设计
- 宾夕法尼亚大学
- 约翰·莫克利（首席顾问）、约翰·埃克特（首席工程师）、约翰·冯·诺依曼

ABC（Atanasoff-Berry Computer，阿塔纳索夫-贝里计算机）：  
- 比 ENIAC 早，1939年完成
- 首台计算机

冯诺依曼提出**存储程序**概念，在《关于 EDVAC 的报告草案》提出了冯诺依曼结构。

EDVAC（Electronic Discrete Variable Automatic Computer，电子离散变量自动计算机）：  
- 1949年8月交付
- 1951年开始运行
- 冯诺依曼结构
- 存储程序
- 指令和数据采用二进制，简化了逻辑线路
- 五个基本部分：
  - 运算器 CA
  - 控制器 CC
  - 存储器 M
  - 输入设备 I
  - 输出设备 O
  - （外部记录介质 R）
- 但第一台冯诺依曼结构计算机是 EDSAC（1949年正式运行）

UNIVAC（UNIVersal Automatic Computer，通用自动计算机）：1951 年交付，1952 年统计选票一战成名，开启了商用计算机时代。

其他经典计算机：  
- 1964 年大型计算机 IBM S/360
- 1964 年超级计算机 CDC 6600
- 1965 年小型计算机 PDP-8
- 1975 年微型计算机 Altair 8800
- 1977 年微型计算机 Apple II
- 1981 年微型计算机 IBM PC 5150

## 模型机

存储器（主存）：  
- 控制总线连接控制逻辑和地址译码器
- 地址总线连接 Memory Address Register（MAR）
- 数据总线连接 Memory Data Register（MDR）

控制器：  
- 指令寄存器（IR）：存当前指令
- 程序计数器（PC）：存下一条指令的地址
- MAR
- MDR
- 指令译码部件
- 控制电路

运算器：  
- ALU：算术运算和逻辑运算
- 通用寄存器
- 状态寄存器

CPU：控制器和运算器

总线：  
- 内部总线：CPU 内部
- 控制总线：控制器的控制电路和存储器控制逻辑
- 地址总线：存储器 MAR 和控制器 MAR
- 数据总线：存储器 MDR 和控制器 MDR

执行指令的四个步骤：取指、译码、执行、回写

输入输出：就像另一个存储器

## 现代计算机

现代计算机结构：  
- CPU
- 北桥：PCIe 控制器、主存控制器、集显
- 南桥：I/O 控制器（硬盘、USB、网卡、PCI 卡、...）

后来，主存控制器、PCIe 控制器、集显（核显）都集成在 CPU 里了。主板只剩下一个 PCH 管 I/O。

再后来发展出了 SOC（System on Chip，片上系统）。

摩尔定律（常见描述）：价格不变，每 18 个月，集成电路上可容纳的晶体管数目翻一番。


# 指令集

简单的指令集示例：  
- 运算类：`ADD R, M`
- 传送类：`LOAD R, M`、`STORE M, R`
- 转移类：`JMP L`
- 格式：4 位操作码 + 4 位寄存器号 + 8 位地址

## x86 体系结构

### 16 位时代：x86-16（IA-16）

Intel 8086：  
- 1978 年发布
- 16 位通用寄存器，也支持 8 位数据
- 16 根数据线
- 20 根地址线，寻址空间 1MB
- 段 + 偏移量寻址
- 代表计算机 IBM 5150（用的是简化版的 8088）

8086 的寄存器模型：  
- 通用寄存器
  - 4 个数据寄存器：AX、BX、CX、DX
    - 每个可分为两个 8 位寄存器使用：AL/AH、BL/BH、CL/CH、DL/DH
  - 指针和变址寄存器：SP、BP、SI、DI
    - 每个对应的低 8 位寄存器：SPL、BPL、SIL、DIL
- 指令指针寄存器：IP
- 标志寄存器：FLAGS
  - 标志位有：
    - 状态标志位：CF（进位）、ZF（零）、SF（符号）、OF（溢出）、PF（奇偶）、AF（辅助进位）
    - 控制标志位：DF（方向）、IF（中断）、TF（跟踪）
- 段寄存器：CS、DS、SS、ES
  - CS：代码段寄存器，指向当前执行的代码段
  - DS：数据段寄存器，指向当前数据段
  - SS：堆栈段寄存器，指向当前堆栈段
  - ES：附加数据段寄存器

8086 的寻址方式：  
- 指令指针寄存器（IP）保存**下一条指令的地址**（即程序计数器）
- 程序员不能直接操作 IP
- 物理地址 = 段寄存器 * 16 + IP
- 1MB 的存储空间分为许多 64KB 的逻辑段，它们可以重叠
- 指令的操作数默认存放在数据段（DS）中，指令地址默认存放在代码段（CS）中，**BP 默认使用 SS 段**

Intel 80286：  
- 24 位地址总线，寻址空间 16MB
- 保护模式，但有缺陷（每个段只有 64KB）
- 实模式（兼容 8086）
- 所有 x86 处理器在加电启动时都首先进入实模式，系统初始化完成后再进入保护模式

### 32 位时代：IA-32

Intel 80386：  
- 8086 系列的首款 32 位处理器
- 32 位通用寄存器
- 32 位地址总线，寻址空间 4GB
- 改进的保护模式：段可达 4GB
  - 保护模式是主要工作模式，支持多任务、特权级、特权指令、访问权限、4GB 物理地址空间、虚拟内存
- 虚拟 8086 模式，可以模拟多个 8086 处理器
  - 寻址空间 1MB，段 + 偏移寻址（与实模式相同）
  - 对中断和异常的处理不同于实模式
  - 用于兼容 8086 系统

IA-32 对寄存器模型的扩展：  
- 通用寄存器（AX -> EAX）
- 指令指针寄存器（IP -> EIP）
- 标志寄存器（FLAGS -> EFLAGS）
- 段寄存器仍然都为 16 位，增加了 FS 和 GS 两个段寄存器

IA-32 的寻址方式：  
- 实模式：CS:IP
- 保护模式：
  - Global Descriptor Table（GDT）：全局描述符表，存放 8192 个段描述符
  - 每个描述符有 8 个字节：[段界限:2, 段基址:3, 权限:1, 其他:1, 段基址:1]
  - 16 位的 CS 寄存器恰好可以寻址 8192 * 8 字节 = 64KB 的 GDT
  - GDTR：GDT 寄存器，在 CPU 内部，存放 GDT 的基址和界限
  - 段基址由根据 GDTR 和 CS，查内存中的 GDT 得到
  - EIP：存放指令的段内偏移地址
- 物理地址 = 段基址 + 段内偏移地址

### 64 位时代：x86-64

AMD Opteron：  
- 首款 64 位 x86 处理器
- 兼容 IA-32，且不降低性能

x86-64 的运行模式：  
- 长模式：64 位 OS
  - 64 位模式：需要重新编译
  - 兼容模式：不需要重新编译
- 传统模式：都不需要重新编译
  - 保护模式：32 位或 16 位 OS
  - 虚拟 8086 模式：32 位或 16 位 OS
  - 实模式：16 位 OS

x86-64 对寄存器模型的扩展：  
- 通用寄存器（EAX -> RAX），并且增加了 R8-R15 寄存器
- 指令指针寄存器（EIP -> RIP）
- 标志寄存器（EFLAGS -> RFLAGS）
- 段寄存器没有变化，仍然为 6 个 16 位寄存器（CS、DS、SS、ES、FS、GS）

x86-64 半废除了 GDT 寻址，GDT 描述符表的基址和界限都为 0，段选择子也不再使用，通过分页寻址。

## x86 指令简介

主要分为传送类、运算类（又可分为算术、逻辑）、控制类、转移类。

字节（BYTE）为 8 位，字（WORD）为 16 位，双字（DWORD）为 32 位，四字（QWORD）为 64 位。

Intel 语法：`MOV WORD PTR [BX + SI*2 + 200H], 01H`  
- 目的在前，源在后
- `[]` 表示内存地址
- `WORD PTR` 表示操作数大小
- `H` 后缀表示十六进制，`D` 表示十进制，`B` 表示二进制，`O` 表示八进制

MOV：  
- 立即数不能为目的
- 不能在存储器之间传送，不能在段寄存器之间传送
- 不能将立即数传送到段寄存器
- CS 不能为目的

栈操作：PUSH、POP

LEA：将地址（而不是地址中的数据）传送到通用寄存器中

算术运算指令：  
- 目的不能是立即数或 CS
- 源和目的不能都是存储器
- 加法：ADD、ADC（目的 + 源 + CF）、INC
- 减法：SUB、SBB（目的 - 源 - CF）、DEC、CMP（DST - SRC，但不写回目的，只设置标志位）
- LEA 不是算术运算指令

逻辑运算指令：  
- 单操作数指令，操作数不能是立即数
- 双操作数指令，限制同 MOV 指令（目的不能是立即数或 CS，源和目的不能都是存储器也不能都是段寄存器，不能将立即数传送到段寄存器）
- NOT、AND
- SHL 逻辑左移（乘 $2^n$）
- SHR 逻辑右移，SAR 算术右移（除以 $2^n$）

转移指令：  
- 无条件转移：
  - 直接转移
    - `JMP SHORT LABEL`：LABEL 是地址，指令中存储的是 IP 到 LABEL 的 8 位偏移量，范围 -128 ~ 127。执行效果：`IP = IP + OFFSET`
    - `JMP NEAR PTR LABEL`：16 位偏移量，范围 -32768 ~ 32767（80386 支持 32 位偏移量）。执行效果：`IP = IP + OFFSET`
    - `JMP FAR PTR LABEL`：指令中存储的是 LABEL。执行效果：`CS = LABEL.CS`、`IP = LABEL.IP`
  - 间接转移：`JMP AX`、`JMP [BX]`、`JMP FAR PTR [SI]`（执行效果如 `IP = [SI]; CS = [SI + 2]`）
- 条件转移：
  - `C`：进位
  - `P`：奇偶
  - `Z`/`E`：零/相等
  - `S`：符号（负数）
  - `O`：溢出
  - `B(E)`、`A(E)`：小于 (等于)/大于 (等于)（无符号数）
  - `L(E)`、`G(E)`：小于 (等于)/大于 (等于)（有符号数）

处理器控制指令：  
- 标志操作
  - STC：设置进位标志 CF
  - CLC：清除进位标志 CF
  - CMC：取反进位标志 CF
- 外同步：HLT（停止 CPU）
- NOP

复杂 x86-64 指令举例：串操作指令  
- 串的基本单位：字节（BYTE）或字（WORD）
- 串长可达 64KB
- 五种串操作指令：MOVS、CMPS、SCAS、LODS、STOS，每种有 B 和 W 两种形式
- 三种重复前缀：REP、REPE、REPNE
- 隐含操作数：
  - 源串：**DS:SI**
  - 目的串：**ES:DI**
  - 串长：**CX**
  - 方向标志 **DF**（0：从低地址到高地址，1：从高地址到低地址）
- 传送过程中，SI、DI、CX 自动更新

**STD 和 CLD 指令**：设置和清除方向标志 DF。

## MIPS 体系结构

设计者：约翰·亨尼西

RISC（Reduced Instruction Set Computer，精简指令集计算机）、CISC（Complex Instruction Set Computer，复杂指令集计算机）

MIPS（Microprocessor without Interlocked Pipeline Stages，微处理器无互锁流水线阶段）：  
- 32 位定长指令
- 指令少、简单
- 仅 Load/Store 指令可访问内存
- 寻址简单
- 需要优秀的编译器

MIPS 的通用寄存器：`$0` - `$31`，分为若干组。

MIPS 的算术指令**不会自动设置标志位**。

MIPS 指令分为 R 型、I 型、J 型：  
- R 型：**ADD rd, rs, rt**、**JR rs**
  - 格式：[6 位 opcode, 5 位 rs, 5 位 rt, 5 位 rd, 5 位 shamt, 6 位 funct]
  - 所有 R 型指令的 opcode 都为 0，funct 决定具体指令
  - shamt：移位量，只对移位指令有效
  - rs、rt：源寄存器，rd：目的寄存器
- I 型：**ADDI rt, rs, imm**、**LW/SW rt, imm(rs)**、**BEQ rs, rt, offset**
  - 格式：[6 位 opcode, 5 位 rs, 5 位 rt, 16 位 immediate]
  - 与 R 型指令的区别：无 shamt、rd、funct
  - rs：源寄存器，rt：目的寄存器
  - immediate：立即数或地址偏移量
- J 型：**J addr**
  - 格式：[6 位 opcode, 26 位 address]
  - 无条件跳转
  - 目的地址 = {PC[31:28], address, 00}，即 **PC 的高 4 位 + 指令中的地址 + 00（低两位）**
  - MIPS 指令 32 位定长，所有指令地址都是 32 位（4 字节）对齐的，所以低两位都是 00

注意操作数顺序。

分支指令：  
- 条件分支：I 型
  - **BEQ/BNE rs, rt, offset**：如果 rs 和 rt 相等（不相等），则跳转到 **(PC + 4) + offset * 4**（注意操作数顺序）
- 无条件分支：J 型和 R 型
  - **J addr**：正负 256MB 跳转
  - **JR rs**：4GB 跳转

# 算术逻辑单元

`add $8, $9, $10` 的执行过程：  
- 取指：指令被取到 IR
- 译码：分析出指令类型、操作数，通过控制电路将加法命令传给 ALU，并将 `$9` 和 `$10` 的输出通过内部总线送到 ALU 的输入
- 执行：
  - ALU：执行加法操作，输出结果
  - 状态寄存器：设置状态标志位（如进位、零、符号、溢出等）
- 回写：将 ALU 的输出通过内部总线送到 `$8` 寄存器

算术运算指令 `add` 和 `sub` 可能引发溢出异常，带后缀 `u` 则不会。（溢出仅针对有符号数，有符号数的溢出会改变符号）  
- 有溢出不一定有进位：四位有符号数加法 `0b0011 + 0b0101 = 0b1000`
- 有进位不一定有溢出：四位无符号数加法 `0b1111 + 0b0001 = 0b0000`。当然也可以说无符号数的进位是无符号数的溢出，因为其结果不在表示范围内。
- 溢出：**最高位的进位输入不等于最高位的进位输出**
  - 溢出其实就是两个正数相加得到负数，或者两个负数相加得到正数
  - 只看最高位的全加器：
    - 若输入为两个正数，即 $A = B = 0$，则进位输出 $C_{out}$ 必然为 $0$；溢出当且仅当 $S = 1$，这要求 $C_{in} = 1$，满足 $C_{in} \neq C_{out}$
    - 若输入为两个负数，即 $A = B = 1$，则进位输出 $C_{out}$ 必然为 $1$；溢出当且仅当 $S = 0$，这要求 $C_{in} = 0$，满足 $C_{in} \neq C_{out}$
    - 即：溢出 $OF = C_{in} \oplus C_{out}$
- MIPS 没有 OF，有符号数溢出会引发异常。x86 不会引发异常，只会设置 OF。

I 型加法指令 `addi` 中的立即数是 16 位的，运算时会有符号扩展为 32 位。它也有不引发溢出异常的版本 `addiu`。

I 型逻辑运算指令 `andi`、`ori` 会对立即数进行零扩展。

## 门电路

晶体管（transistor）：现代集成电路通常用 Metal-Oxide-Semiconductor (MOS) 晶体管。  
- NMOS：当 Gate 为高电平时，Drain 和 Source 之间导通
  - NMOS 的 Gate 和 Source 之间没有小圆圈
- PMOS：当 Gate 为低电平时，Drain 和 Source 之间导通
  - PMOS 的 Gate 和 Source 之间有小圆圈。
- 助记：有小圆圈 -> 0 -> 不导通
- 两种晶体管组合成 Complementary MOS (CMOS) 晶体管
- 非门、与非门、与门、或门、异或门

## 寄存器

D 触发器（D Flip-Flop）：  
- 有存储能力的基本单元，由逻辑门构成
- 一个数据输入，一个时钟输入，一个输出
- 在时钟上升沿时，数据输入的值被锁存到输出中。在其他时刻，无论数据输入的值如何变化，输出都保持不变。
- **CLK-to-Q 延迟**：时钟上升沿到输出稳定的时间
- **Setup/Hold 时间**：数据输入在时钟上升沿前/后必须保持稳定的时间
  - 类比：显示器和相机
- 两个 D 触发器连起来，如果它们的时钟上升沿同时到达，则第一个触发器的输出不一定和第二个触发器的输出一致（可能是第一个触发器先前的输出）。
- 32 个 D 触发器并起来，就是一个 32 位寄存器

## 加法器

32 位与运算：并联 32 个与门。

将不同的 32 位运算单元再并联起来，再加上一个多选器选择运算结果，就可以实现 ALU。

半加器（Half Adder）：将两个一位二进制数相加，输出和与进位。  
- 注意到 $S = A + B = A \oplus B$，$C = A \cdot B$
- **半加器就是一个异或门和一个与门**

全加器（Full Adder）：将两个一位二进制数和一个进位相加，输出和与进位。  
- 注意到 
  - $S = A + B + C_{in} = (A \oplus B) \oplus C_{in}$
  - $C_{out} = A \cdot B + (A \oplus B) \cdot C_{in}$
- 由**两个半加器和一个或门**组成
- 把 32 个全加器串联起来，前一个的进位输出连接下一个的进位输入，就可以实现 32 位加法器

减法：转换为加法，$A - B = A + (-B) = A + \sim B + 1$  
- 在先前的 32 位加法器的第二个输入前添加一个 2-to-1 多选器
- 多选器的选择信号连接到最低位全加器的进位输入
- 如果多选器的选择信号为 $0$，则输出为 B，正常计算 $A + B$
- 如果多选器的选择信号为 $1$，则输出为 $\sim B$，计算减法 $A + \sim B + 1$，即 $A - B$（$1$ 来自最低位全加器的进位输入）

先前的加法器是行波进位加法器（Ripple Carry Adder），优点是简单，缺点是高位运算必须等待低位运算完成，延迟累积。  
- 记门延迟为 $T$，线延迟为零，则 32 位加法器的总延迟是 $3T + 31\times 2T = 65T$
  - 第 $i$ 位的进位信号依赖于第 $i-1$ 位的进位信号
  - 最低位的进位信号需要 $3T$
  - 更高位的进位信号需要 $2T$
- 需要提前计算进位信号

注意到 

$$C_{i+1} = (A_i \cdot B_i) + (A_i \cdot C_i) + (B_i \cdot C_i),$$

设  
- $G_i = A_i \cdot B_i$：生成（Generate）信号
- $P_i = A_i + B_i$：传播（Propagate）信号
- 这两个信号都由 $A$ 和 $B$ 决定，可以并行地计算出来，延迟不会累积

则 $$C_{i+1} = G_i + P_i \cdot C_i.$$

将这个式子不断迭代展开，可以得到 $C_{32}$ 的表达式：

$$
\begin{align*}
C_{32} &= G_{31} \\
&+ P_{31} \cdot G_{30} \\
&+ P_{31} \cdot P_{30} \cdot G_{29} \\
&\vdots \\
&+ P_{31} \cdot P_{30} \cdots P_1 \cdot G_0 \\
&+ P_{31} \cdots P_1 \cdot P_0 \cdot C_0
\end{align*}
$$

此时，进位信号的延迟固定为 $3T$：  
- 从 $A, B$ 计算 $G, P$ 为 $T$
- 展开式中的与运算延迟为 $T$
- 最后的或运算延迟为 $T$
- 每一位的运算都是并行的
- 缺点：电路复杂，位数越多越复杂
- 整个加法器的延迟为 $3T + T = 4T$（最高位进位信号通过最后一个全加器需要 $T$ 的延迟）

改进后的加法器称为超前进位加法器（Carry-Lookahead Adder, CLA）。32 位完全的超前进位加法器的延迟为 $4T$，但电路过于复杂难以实现，一般使用多个小 CLA 用行波进位的方式连接起来。  
- 设门延迟为 $0.02ns$，
- 32 位 RCA 的延迟为 $65 \times 0.02 = 1.3ns$
- 32 位 CLA 的延迟为 $4 \times 0.02 = 0.08ns$
- 4 级 CLA 的延迟为 $(3\times 0.02) \times 4 + 0.02 = 0.26ns$

# 乘法器与除法器

## 乘法器

只有零和一的十进制乘法和二进制乘法是一样的。

$N$ 位乘法器的组成：  
- **被乘数寄存器：$2N$ 位，带左移，低 $N$ 位存放被乘数**
- **乘数寄存器：$N$ 位，带右移**
- **积寄存器：$2N$ 位**
- **加法器：$2N$ 位**

$N$ 位乘法器的工作流程：  
1. 检查乘数寄存器最低位：
  - 如果为 $1$，将被乘数寄存器的值加到积寄存器中
  - 这个分支实际上可以取消：相当于先将乘数寄存器最低位和被乘数取与，然后再加到积寄存器中
2. 将被乘数寄存器的值左移一位
3. 将乘数寄存器的值右移一位
4. 如果这是第 $N$ 次循环，则结束；否则，返回第 1 步

时间优化（性能优化）：  
- 分析：加法、左移、右移都需要一个时钟周期，$N$ 位乘法将需要约 $3N$ 个时钟周期。
- 优化：**加法和移位操作并行**
  - 控制逻辑同时给出加法、左移和右移信号，在下一个时钟上升沿时，这三个操作就能并行完成
  - 被乘数寄存器的移位不会影响加法，因为移位操作完成时，被乘数寄存器的值已经发给输出端口了
- 现在，乘法器只需要 $N$ 个时钟周期。

空间优化（面积优化）：  
- 分析：
  - 被乘数寄存器有 $2N$ 位，但其中有效位只有 $N$ 位
  - 乘数寄存器有 $N$ 位，但每次右移后，它的有效位就减少一位
  - 乘积寄存器有 $2N$ 位，但每次加法的有效数字只有 $N$ 位
  - 加法器 $2N$ 位，但每次加法的有效数字只有 $N$ 位
- 优化：
  - 被乘数寄存器缩减为 $N$ 位，不左移
  - 乘积寄存器仍然为 $2N$ 位，初始有效数字存放在高 $N$ 位，随运算过程不断右移
  - 加法器也被自然缩减为 $N$ 位，每次取出乘积寄存器的高 $N$ 位与被乘数寄存器相加
  - 取消乘数寄存器，将乘数放在乘积寄存器的低 $N$ 位中，并将其与控制逻辑相连（随着右移，这低 $N$ 位的乘数逐渐被移出）
- 优化后乘法器的组成：
  - **被乘数寄存器：$N$ 位，存放被乘数**
  - **乘积寄存器：$2N$ 位，带右移，高 $N$ 位存放乘积，低 $N$ 位存放乘数**
  - **加法器：$N$ 位**


## 除法器

只有零和一的十进制除法和二进制除法是一样的。

带符号数的除法，余数和被除数符号相同。$-9 \div 4 = -2 ... -1$

$N$ 位除法器的组成：  
- **余数寄存器：$2N$ 位，存放 $2N$ 位的被除数（余数）**
- **除数寄存器：$2N$ 位，带右移，高 $N$ 位存放除数**
- **商寄存器：$N$ 位，带左移**
- ALU：$2N$ 位，支持加法和减法

$N$ 位除法器的工作流程：  
1. 余数 = 余数 - 除数
2. 检查余数最高位：
   - 若为 $1$，即余数为正，则商寄存器左移一位，并将最低位置 $1$
   - 若为 $0$，即余数为负，则将除数加回余数（回滚），商寄存器左移一位
3. 除数寄存器右移一位
4. 如果这是第 $N$ 次循环，则结束；否则，返回第 1 步

空间优化（面积优化）：  
- 分析：
  - 除数寄存器有 $2N$ 位，但其中有效位只有 $N$ 位
  - 商寄存器初始时空，随着运算过程逐渐填满
  - 余数寄存器初始时满，随着运算过程逐渐清空
- 优化：
  - 除数寄存器缩减为 $N$ 位，不右移
  - 取消商寄存器，商从低位逐位移入余数寄存器
  - ALU 也被自然缩减为 $N$ 位，每次取出余数寄存器的高 $N$ 位与除数寄存器做加减法
  - 运算完成后，余数寄存器的高 $N$ 位就是余数，低 $N$ 位就是商（**余数寄存器还需要支持右移，从而取出余数**）
- 优化后除法器的组成：
  - **余数寄存器：$2N$ 位，带左移、右移，存放余数和商**
  - **除数寄存器：$N$ 位**
  - ALU：$N$ 位，支持加法和减法

这种除法器不容易做时间优化了，它不像乘法器那样可以并行地做加法和移位。


# 单周期处理器

简化 MIPS 指令集：  
- 无符号加减法：
  - `addu rd, rs, rt`
  - `subu rd, rs, rt`
- 立即数的逻辑或：
  - `ori rt, rs, imm16`
- 加载存储：
  - `lw rt, imm16(rs)`
  - `sw rt, imm16(rs)`
- 条件分支：
  - `beq rs, rt, imm16`

以上指令中的立即数都是 16 位的，**运算时都需要扩展为 32 位**。其中 `ori` 为零扩展，其他均为符号扩展。

指令集的硬件需求：  
- **ALU**
  - 支持加法、减法、逻辑或、比较相等
  - 操作数：2 个 32 位数，来自寄存器或 32 位立即数
- **立即数扩展部件**
  - 将 16 位立即数扩展为 32 位
  - 符号扩展和零扩展
- **PC**
  - 一个 32 位寄存器
  - 两种加法：
    - PC + 4：取指令地址
    - PC + 4 + imm16 * 4：分支跳转地址
- **寄存器堆**
  - 32 个 32 位寄存器
  - 两个读端口，一个写端口
- **存储器**
  - 只读的指令存储器（32 位地址，32 位数据）
  - 读写的数据存储器（32 位地址，32 位数据）

寄存器堆：  
- 32 个 32 位寄存器
- 数据接口信号：
  - busA, busB：32 位读端口
  - busW：32 位写端口
- 读写控制：
  - Ra, Rb：5 位寄存器编号，分别对应 busA 和 busB
  - Rw：5 位寄存器编号，对应 busW
  - WriteEnable：写使能信号，在时钟信号 clk 的上升沿，若 WriteEnable == 1，则将 busW 写入对应寄存器
- 注意：寄存器堆的读操作不受时钟控制

存储器：  
- 数据接口信号：
  - Data In：32 位数据输入
  - Data Out：32 位数据输出
- 读写控制
  - Address：32 位地址
  - WriteEnable：写使能信号，在时钟信号 clk 的上升沿，若 WriteEnable == 1，则将 Data In 写入 Address 指向的存储单元
- 一个读端口，一个写端口

## 数据通路

取指单元（Instruction Fetch Unit, IFU）：  
- 组成部件
  - PC：程序计数器
  - 指令存储器
  - 两个加法器
  - 多选器：选择 PC + 4 或分支跳转地址
  - 与门：跳转当且仅当 `nPC_sel == 1` 且 `zero == 1`
  - 立即数符号扩展并左移部件
- 输入：
  - 时钟信号 clk 
  - 分支跳转信号 nPC_sel
  - ALU 的比较结果信号 zero
- 输出：
  - 指令

无论跳转指令 `beq rs, rt, imm16` 的条件是否成立，PC 都会加 4。如果成立，则 PC 会被继续加上 `SignExt[imm16] * 4`。

译码、执行、访存、写回单元：  
- 问题
  - 指令的目的寄存器（要写的寄存器）有时是 rd，有时是 rt
  - ALU 的第二个输入有时是寄存器堆的输出，有时是扩展后的立即数
  - 寄存器的 busW 有时是 ALU 的输出，有时是数据存储器的输出
- 组成部件
  - 寄存器堆
  - ALU
  - 立即数扩展部件
  - 多选器：从 rd 和 rt 中选择寄存器堆 busW
  - 多选器：从立即数扩展部件输出和寄存器堆 busB 中选择 ALU 的第二个输入
  - 数据存储器
  - 多选器：从 ALU 输出和数据存储器输出中选择寄存器堆 busW
- 输入：
  - 时钟信号 clk
  - 立即数
  - 控制信号
    - n_PC_sel："+4" 或 "branch"
    - ALUCtr："ADD"、"SUB"、"OR"
    - ALUSrc：ALU 的第二个输入的选择信号，"0" 为寄存器堆 busB，"1" 为立即数扩展部件的输出
    - RegWr：寄存器堆写使能信号
    - RegDst：寄存器堆写端口选择信号，"0" 为 rt，"1" 为 rd
    - ExtOp：立即数扩展的选择信号，"0" 为零扩展，"1" 为符号扩展
    - MemtoReg：寄存器堆 busW 选择信号，"0" 为 ALU 的输出，"1" 为数据存储器的输出
    - MemWr：数据存储器写使能信号（是否将 busB 写入数据存储器）

## 控制信号

R 型运算指令的控制信号：以 `addu rd, rs, rt` 为例：  
- nPC_sel："+4"
- ALUSrc："0"，ALU 的第二个输入来自寄存器堆 busB
- ExtOp: 任意
- ALUCtr："ADD"
- MemWr: "0"
- MemtoReg: "0"，寄存器堆 busW 选择 ALU 的输出
- RegWr: "1"
- RegDst: "1"，寄存器堆写端口选择寄存器 rd

I 型运算指令的控制信号：以 `ori rt, rs, imm16` 为例：  
- nPC_sel："+4"
- ALUSrc："1"，ALU 的第二个输入来自立即数扩展部件
- ExtOp: "0"，立即数扩展为零扩展
- ALUCtr："OR"
- MemWr: "0"
- MemtoReg: "0"，寄存器堆 busW 选择 ALU 的输出
- RegWr: "1"
- RegDst: "0"，寄存器堆写端口选择寄存器 rt

加载指令的控制信号：以 `lw rt, imm16(rs)` 为例：  
- nPC_sel："+4"
- ALUSrc："1"，ALU 的第二个输入来自立即数扩展部件
- ExtOp: "1"，立即数扩展为符号扩展
- ALUCtr："ADD"
- MemWr: "0"
- MemtoReg: "1"，寄存器堆 busW 选择数据存储器的输出
- RegWr: "1"
- RegDst: "0"，寄存器堆写端口选择寄存器 rt

跳转指令的控制信号：以 `beq rs, rt, imm16` 为例：  
- nPC_sel："branch"
- ALUSrc："0"，ALU 的第二个输入来自寄存器堆 busB
- ExtOp: 任意
- ALUCtr："SUB"
- MemWr: "0"
- MemtoReg: "x"
- RegWr: "0"
- RegDst: "x"

## 控制信号的集成

指令类型的控制信号可以由 opcode 和 func 只经**逻辑与**得到：  
- rtype = ~op5 & ~op4 & ~op3 & ~op2 & ~op1 & ~op0
  - 即 op 的 6 位都为 0
- add = rtype & func5 & ~func4 & ~func3 & ~func2 & ~func1 & ~func0
  - 即指令为 R 型且 func 为 100000
- sub = rtype & func5 & ~func4 & ~func3 & func2 & func1 & ~func0
  - 即指令为 R 型且 func 为 100010
- 其余指令同理

控制 CPU 执行的 8 个控制信号可以由指令类型的控制信号只经**逻辑或**得到：  
- nPC_sel = beq
- ALUSrc = ori | lw | sw
- ExtOp = lw | sw
- ALUCtr[0] = sub | beq
- ALUCtr[1] = ori
- MemWr = sw
- MemtoReg = lw
- RegWr = add | sub | ori | lw
- RegDst = add | sub

其中 ALUctr 有两位：  
- 00：加法
- 01：减法
- 10：逻辑或

# 控制器的基本原理

控制器接受指令的二进制编码，给出控制信号。

简单的指令集示例：  
- 运算类：`ADD R, M`
- 传送类：`LOAD R, M`、`STORE M, R`
- 转移类：`JMP L`
- 格式：4 位操作码 + 4 位寄存器号 + 8 位地址

模型机的组成：  
- 控制电路、指令译码电路
- IR
- PC
- MAR
- MDR
- 存储器

控制信号：  
- $IR_{IN}$：是否写入 IR
- $IR_{OUT}$：是否从 IR 读出操作码
- $PC_{OUT}$：是否从 PC 读出地址
- $PC_{加}$：是否将 PC 自增
- $MAR_{IN}$：是否写入 MAR
- $MDR_{OUT}$：是否从 MDR 读出数据
- $R1_{IN}$：是否写入 R1
- $M_{读}$：是否从存储器读出数据

`LOAD R1, M1` 指令的执行过程：  
1. 取指：
   1. 控制器发 $PC_{OUT}$ 和 $MAR_{IN}$ 信号，将 PC 的值送入 MAR
   2. 控制器发 $M_{读}$ 信号，从存储器中读出指令，送入 MDR
   3. 控制器发 $PC_{加}$ 信号，将 PC 加上指令长度
   4. 控制器发 $MDR_{OUT}$ 和 $IR_{IN}$ 信号，将 MDR 的值送入 IR
2. 执行：
   1. 控制器发 $IR_{OUT}$ 和 $MAR_{IN}$ 信号，将 IR 的地址部分送入 MAR
   2. 控制器发 $M_{读}$ 信号，从存储器中读出数据，送入 MDR
   3. 控制器发 $MDR_{OUT}$ 和 $R1_{IN}$ 信号，将 MDR 的值送入 R1

## 硬布线控制器

MIPS、PowerPC、ARM 采用硬布线控制器；奔腾采用硬布线 + 微程序控制器。

硬布线控制器又叫硬连线控制器或组合逻辑控制器，主要部件有：  
- 环形脉冲发生器：产生节拍脉冲信号（哪个时钟周期）
- 指令译码器（哪个指令）
- 微命令编码器：在不同节拍脉冲信号的同步下产生微命令信号（控制信号）

不同指令在不同机器周期应发出的控制信号：

| 指令  | T1  | T2 | T3 | T4 | T5 | T6 | T7 |
| :---: |:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| LOAD (00) | $PC_{OUT}, MAR_{IN}, M_{读}, PC_{加}$ | $MDR_{OUT}, IR_{IN}$ | $IR_{OUT}, MAR_{IN}, M_{读}$ | $MDR_{OUT}, R1_{IN}$ | End |  |  |
| ADD (01) | 同上 | 同上 | $IR_{OUT}, MAR_{IN}, M_{读}$ | $MDR_{OUT}, Y{IN}$ | $R1_{OUT}, add$ | $Z_{OUT}, R1_{IN}$ | End |
| STORE (10) | 同上 | 同上 | $IR_{OUT}, MAR_{IN}$ | $R1_{OUT}, MDR_{IN}, M_{写}$ | End |  |  |
| JMP (11) | 同上 | 同上 | $IR_{OUT}, PC_{IN}$ | End |  |  |  |

于是，控制信号 $MAR_{IN}$ 可以表示为：

$$
\begin{align*}
MAR_{IN} &= T1 \cdot (LOAD + ADD + STORE + JMP) + T3 \cdot (LOAD + ADD + STORE) \\
&= T1 + T3 \cdot (LOAD + ADD + STORE) \\
\end{align*}
$$

据此可以作出微命令编码器的逻辑电路。

硬布线控制器：  
- 优点：指令执行速度快
- 缺点：
  - 控制逻辑复杂
  - 设计、验证困难
  - 扩充、修改困难

## 微程序控制器

机器指令的执行过程可以分为多个微操作（micro-operation），每个微操作对应一个微指令（micro-instruction）。微指令存储在 CPU 内一个只读存储器中。

模型机有 16 个控制信号，微指令的微操作字段有 16 位，每一位都是一个控制信号，下址字段有 5 位，表示下一条微指令的微地址。若干微指令就组成了一个微程序。

微指令：  
- 微操作字段：16 位，每一位就是一个控制信号
- 下址字段：5 位
  - 最高位：是否分支（取指用的微指令的最后一条微指令需要，其下址字段需要根据译码得到的机器指令填充）
  - 低四位：下一条微指令地址

在开始执行机器指令时，先从控制存储器中读出**取指用的微指令**（微程序的公用部分），然后根据机器指令的操作码，找到对应的微程序入口地址，开始执行微程序，从而完成机器指令的执行。

微程序控制器：  
- 优点：
  - 规整：用程序来产生和组织微命令信号，用存储逻辑代替组合逻辑
  - 灵活：可扩展、可修改
- 缺点：
  - 速度慢

# 流水线处理器

五阶段：取指、译码、执行、访存、写回。

因为流水线寄存器的存在，流水线没有缩短单条指令的延迟，而是提高了吞吐量。

流水线可能不平衡：如果不同计算阶段的延迟不相等，则时钟周期不得不被设置为最长的延迟，以保证所有阶段都能完成。  
- 注意：不平衡的流水线的延迟是**最长阶段延迟乘以流水线阶段数**，而不是**所有阶段延迟之和**
- 将长延迟的阶段拆分成两个短延迟的阶段
- 随着流水线越来越深，流水线寄存器的延迟的影响会越来越大，且越来越复杂

有一个五级流水线的处理器设计，每一级的延迟分别为：450 ps，950 ps，450 ps，450 ps，450 ps。其中包含流水线寄存器延迟 50 ps。如果将第二级（950 ps）均匀拆分为两级，从而形成六级流水线。那在新处理器上，一条指令的执行时间是：3000 ps。

超标量（Superscaler）处理器：有两条或以上并行的流水线的处理器。

从单周期到流水线，是时间并行性的提升；从单流水线到超标量，是空间并行性的提升。

第一台超级计算机 CDC6600 已经有了超标量的雏形。

## 流水线冒险

流水线的三种冒险：  
- 结构冒险
- 数据冒险
- 控制冒险

结构冒险：由于**硬件资源不足**，流水线的某个阶段不能同时处理多条指令。  
- 如果指令和数据在同一存储器中，则不能同时访存和取指
  - 解决方法：bubble 或指令存储器与数据存储器分离
- 写回阶段和译码阶段都需要访问寄存器堆
  - 解决方法：寄存器读写速度很快，前半个时钟周期写，后半个时钟周期读

数据冒险：由于**数据依赖**，流水线的某个阶段不能同时处理多条指令。  
- `sub $t0, $s1, $s2; add $s3, $t0, $s4`，第二条指令需要等待第一条指令的结果
  - 正在译码的指令要读的寄存器还没有被先前的指令写回
  - 但译码并不真正用到读出的寄存器数据，所以更确切的说法是
  - **即将在执行阶段用到的寄存器数据还没有被先前的指令写回**
  - 解决方法：
    - bubble 两次（相当于在两条指令之间插入两个 `nop`）
    - 数据前递：**将访存阶段的 ALU 输出转发到执行阶段的 ALU 输入**，避免了 bubble
- `sub $t0, $s1, $s2; add $s3, $t0, $s4; and $s5, $s4, $t0`
  - 解决方法：
    - **将写回阶段的 ALU 输出转发到执行阶段的 ALU 输入**
- load-use 冒险：`lw $t1, 20($t0); or $t2, $t1, $s5`
  - **`or` 在执行阶段需要的操作数需要在 `lw` 的访存阶段完成后才能得到**
  - 解决方法：
    - bubble 一次（假设已经实现了数据前递）

控制冒险：由于**分支指令**，流水线的某个阶段不能同时处理多条指令。  
- `j Target`：
  - 目标地址在分支指令的取指阶段就可以获得，不需要 bubble
- `jr rs`：
  - 目标地址在分支指令的译码阶段才能获得，需要 bubble 一次
- `beq rs, rt, imm16`：
  - 我们可以在译码阶段增加逻辑，判断 `rs` 和 `rt` 是否相等
  - 目标地址在分支指令的译码阶段才能获得，需要 bubble 一次
- 延迟转移技术：**规定分支指令后面的一条指令一定会被执行**，即使分支不成立。

# 存储层次结构

冯诺依曼架构中的运算器、存储器以及外部记录介质都含有存储部件。

存储器特性：非易失性、可读可写、随机访问、访问时间、容量、价格、功耗

> 如果有一个非易失、可读可写、随机访问、访问时间短、容量大、价格低、功耗低的存储器，那就可以整个计算机系统只用它，而不需要存储层次结构了。

## DRAM

一个**内存模组**（内存条）上有若干 **DRAM 芯片**：  
- 每个 DRAM 芯片上有若干 **存储单元**（超单元），由行地址和列地址访问，一个超单元可存储 4 或 8 个比特
- 存储单元由电容和一个晶体管组成，电容会漏电，因此每隔一段时间需要刷新
- SDRAM: 同步 DRAM。DDR SDRAM 是它的升级

**取出地址 $A$ 中的一个字**：  
- 流程：
  - CPU 通过系统总线将地址 $A$ 发送到内存控制器
  - 内存控制器将 $A$ 转化成行列地址 $(i, j)$，发送到 $A$ 所在的内存模组
  - （对 DRAM 芯片**预充电**）
  - **ACT**（ACTivate）或 **RAS**（Row Access Strobe）：内存控制器将 $i$ 广播给其所有 DRAM 芯片。DRAM 芯片的行译码器将 $i$ 译码，选中对应的行，将行数据经放大放在行缓冲区中。**此过程需要 tRCD（Row to Column Delay）长的时间**
  - **CAS**：内存控制器将 $j$ 广播给其所有 DRAM 芯片。DRAM 芯片从行缓冲区中取出 $j$ 列的数据并输出。**此过程需要 CL（CAS Latency）个时钟周期**
  - 内存控制器采样数据，经过系统总线送入 CPU
- CAS 可以读多个数，每个时钟周期都能输出一个数 
- 如果下一次访问的数据在同一行，则可以省略 RAS，减少 tRCD 的延迟
- 否则预充电，将这一行关闭
- 预充电既可以在选中新行时进行，也可以在一次传输只剩最后一个字时进行（可以和最后一个字的传输重叠）
- 性能参数：
  - **tRCD**（Row to Column Delay）：从 RAS 到 CAS 的**时间**，约 2~3 个时钟周期（15~23ns）
  - **CL**（CAS Latency）：从 CAS 到数据输出的**时钟周期数**，约 2~3 个时钟周期
  - **tRP**（Row Precharge）：从预充电到 RAS 的**时间**，约 2~3 个时钟周期
- 两次行地址不同的 SDRAM 读需要间隔 tRCD/时钟周期 + CL + 数据个数 - 1 + tRP/时钟周期 个时钟周期

SDR 只在时钟上升沿传输和采样数据，DDR（Double Date Rate）在时钟上升沿和下降沿都传输和采样数据。

以送出四个数据为例：  
- SDR 需要 tRCD + CL + 4 个时钟周期
- DDR 需要 tRCD' + CL' + 2 个时钟周期
- DDR 厂商宣传的是 2 个数据每时钟周期的访存带宽，但由于时钟周期相比 SDR 变长，访存延迟（读出第一个数据的延迟）没有优势
- 不过后来工艺不断改进，SDR 就被淘汰了

DDR-200 的核心频率为 100MHz（和 SDRAM PC100 相同），每次从核心取出 2 个数据，I/O 频率仍为 100MHz 不变（受主板限制），等效频率为 200MHz（每个 I/O 周期传输 2 个数据，100MHz * 2 = 200MHz）。

DDR2-400 核心频率仍为 100MHz，但每次从核心取出 4 个数据，I/O 频率提升到了 200MHz，等效频率为 400MHz（每个 I/O 周期传输 2 个数据，200MHz * 2 = 400MHz）。

DDR3-2133 核心频率 266MHz，每次从核心取出 8 个数据，I/O 频率提升到了 1066MHz，等效频率为 2133MHz（每个 I/O 周期传输 2 个数据，1066MHz * 2 = 2133MHz）。

DDR4 发现从核心取出 16 个数据意义很小（多取的数据基本没用）：Bank Grouping 技术  
- 在发第一个访存地址和第一组数据返回之间，发第二个访存地址（需要属于不同的 bank）

DDR5 从核心取 16 个数据。

$$等效频率 = I/O频率 \times 2$$

$$I/O 频率 = 核心频率 \times 数据预取个数 / 2$$

$$带宽 = 等效频率 \times 64bit$$

每次 DDR 的换代基本都伴随着行选、列选延迟的提高，这一提高在代内的升级过程中又被逐渐改进。多年来，此延迟的提升非常有限，这主要被超单元中电容充放电的物理特性约束。

## SRAM

SRAM：  
- SRAM 的一个存储单元由 6 个晶体管组成，是一个**双稳态触发器**。它只能存储一个比特
- $BL$ 和 $\bar{BL}$ 传送读写数据信息，$WL$ 控制此存储单元是否可访问
- $VDD$ 是电源电压，$GND$ 是地线
- 写入 $1$ 时：
  - $BL = 1, \bar{BL} = 0, WL = 1$
  - 当晶体管状态稳定后，令 $WL=0$，则晶体管 $M5, M6$ 关闭，$BL$ 和 $\bar{BL}$ 信号撤销
  - 此时 SRAM 内部仍然能保持刚刚的状态，写入的这个 $1$ 被保存在 $Q$ 点。只要 $WL=0$，就能一直保持这个状态（当然，前提是通电）
- 读取 $1$ 时：
  - $WL = 1$，则 $BL$ 和 $\bar{BL}$ 就被 SRAM 自然置为 $1$ 和 $0$
- 组成：
  - 地址线、数据线、读写控制线、片选线
  - 译码驱动电路、读写控制电路
  - 存储矩阵

相比 DRAM，SRAM 的速度快、功耗高、集成度低（每位晶体管数多）、价格高、不用刷新

## Cache

时间局部性：最近访问的指令或数据很快还会被访问

空间局部性：访问的指令或数据附近的指令或数据也很快会被访问

循环有很好的时间局部性和空间局部性。数组访问有很好的空间局部性。

Cache 会一次取回一整块数据（空间局部性），并将其暂时存储在 Cache 中（时间局部性）。

内存地址被分割为三部分：  
- 标记位
- 组索引
- 块偏移
  
Cahe 的每个表项包含：  
- 有效位
- 标记位
- 数据块

Cache 先根据组索引找到对应的组，再检查组中每一行（本课示例中为直接映射高速缓存，每组仅一行）的标记位与有效位，如果匹配成功，则缓存命中，否则向主存请求数据，填入 Cache 中。

Cache 的写策略：  
- 缓存命中：
  - 写穿透：将数据写入 Cache 和主存
  - 写返回：懒惰写入，只将数据写入 Cache，直到数据被替换时才写入主存（利用时间局部性，多次写入可以只写最后一次）
- 缓存未命中：
  - 写不分配：直接将数据写入主存
  - 写分配：将数据读入 Cache，再写入 Cache（利用时间局部性，写入后可能被再次访问）
- 写返回和写分配一般结合使用，适合复杂高效的系统
- 写穿透和写不分配一般结合使用，适合简单的系统

Cache 的平均访问时间：

$$Average Memory Access Time = Hit Time + Miss Rate \times Miss Penalty$$

Cache 的失效原因：  
- 义务失效（Compulsory Miss）：冷访问，Cache 中没有数据
  - 无法避免
- 容量失效（Capacity Miss）：Cache 容量不足，数据被替换
  - 可以通过增大 Cache 容量缓解
- 冲突失效（Conflict Miss）：Cache 中有足够的空间，但多个数据映射到同一行
  - 可以通过增加组相联路数缓解

组相联路数越多，cache 越复杂，命中时间越长。

Cache 替换算法：随机、round-robin、LRU（Least Recently Used）

Cache 实例：  
- 80486 指令和数据共用 cache，指令和数据的局部性互相影响
- Pentium 指令和数据分开 cache
- Core I7 多级 cache

## 单位

除了计算机内部的存储器（cache、内存）以 $1024$ 为底，其他单位都以 $1000$ 为底。  
- 计算机内部的存储器（cache、内存）的单位以 $1024$ 为底
- 外部存储器（硬盘、优盘）的单位以 $1000$ 为底
- **网速** Mbps、**时钟频率** GHz、SATA **带宽** Gbit/s 以 $1000$ 为底
- Ki、Mi、Gi 前缀专门表示以 $1024$ 为底的单位


# 中断和异常

第一个带有异常处理的系统：1951 年 UNIVAC
- 算术运算溢出时：转向地址 $0$ 执行两条修复指令，或者停机

第一个带有外部中断的系统：1954 年 DYSEAC
- 有两个程序计数器，根据 I/O 信号进行切换

中断（异常）的叫法种类很多：  
- 外部事件产生的：外部中断、硬件中断、中断
- 内部事件产生的：内部中断、软件中断、异常
- 二者统称中断，也可统称异常

8086 的存储器中除了存放一般数据和指令的区域外，还保留了两个专用区域：  
- **中断向量表区**：00000H~003FFH，低 1KB 地址
  - **中断向量：中断服务程序的入口地址**
  - 256 个中断向量，每个中断向量 4 字节
  - 中断向量：**前两个字节存偏移量，后两个字节存段基址。小端法（低字节在前）**
- 初始化程序区：FFFF0H~FFFFFH，高 16B 地址
  - CPU 复位后从 FFFF0H 取出第一条指令，通常是到系统程序入口的无条件转移
- 256 个中断中，有 5 个是专用的，27 个是保留的，其余 224 个都由用户定义
- 如果中断类型码是 17H，中断服务程序入口地址是 2340H:7890H，则中断向量存放在 5CH，中断向量内容为 [90H, 78H, 40H, 23H]。

保护模式下，虽然逻辑地址仍然写成段基址:偏移量的形式，但物理地址的产生方式不同了。（参考 IA-32 的寻址方式）  
- 保护模式的中断向量表（中断描述符表 IDT）可以在内存任意地方，存放 256 个 8 字节的中断描述符
- IDT 的地址存放在 CPU 内部的 IDTR 寄存器中
- 中断描述符中包括 16 位的段选择符和 32 位的偏移地址，它们会被分别加载到 CS 和 EIP 寄存器中
- 中断描述符的地址 = 中断类型号 * 8 + IDTR 中的基址

中断处理过程：  
1. CPU 关闭中断响应（置零标志寄存器 FLAGS 中的 IF 位）
  - STI 指令和 CLI 指令
   - 对内部中断和非屏蔽中断无效
2. 压栈保存断点（可能是发生中断的指令的地址，也可能是其下一条指令的地址）
3. 确定中断类型号
4. 保存现场（有关寄存器和标志寄存器）
5. 跳转到中断服务程序开始执行（可以适当时刻开放中断响应，以免过长时间关闭中断）
6. 恢复现场并返回
  - IRET 指令：弹栈 3 个字，分别放入 IP、CS、FLAGS 寄存器中（后来又有针对 32 位、64 位的 IRETD、IRETQ）
- 通常前三步由硬件完成，后三步由软件（中断服务程序）完成

## 内部中断

内部中断：由 CPU 内部产生的中断  
- **除法错中断**（0）：商超出目标寄存器范围，如除以零
- **溢出中断**（4）：执行 INTO 指令（等价于 INT 4）时，若 OF 为 1，则引起溢出中断
- **单步中断**（1）：当 TF 为 1 时，CPU 每执行一条指令就自动引起一个单步中断。单步中断服务程序一般会显示各寄存器内容等信息，用于调试
  - 单步中断可以通过软件方法屏蔽
- **断点中断**（3）：
  - INT 3 指令是唯一单字节长的 INT 指令，其他 INT 指令都是双字节长的（0~256 的中断类型码就需要 8 位）
  - 使用步骤：
    - 用 INT 3 替换用户程序欲断点位置的原有指令，保存此原有指令
    - 用户程序运行到此处时便会执行 INT 3，进入断点中断服务程序
    - 断点中断服务程序返回前，恢复原有指令，并将 IP 减 1
    - 返回后，用户程序会从断点处继续执行原有指令
  - 单字节长保证了 INT 3 不会替换两个用户指令，导致控制流被扰乱

内部中断的特点：  
- 中断类型号都在 CPU 内部产生（外部中断需要从外部读取）
- 除单步中断可以通过软件设置 TF 控制外，所有内部中断都不能用软件方法屏蔽
- 除单步中断外，所有内部中断的优先级都比外部中断高

INT n 指令：  
1. 压栈 FLAGS
2. 清除中断标志 IF 和单步标志 TF
3. 压栈 CS 和 IP
4. 根据中断类型码 n 查中断向量表，取出中断服务程序的段基址和偏移量
5. 将 CS 和 IP 寄存器分别装入中断服务程序的段基址和偏移量

BIOS 存放在 ROM 中，提供系统加电自检、引导、主要 I/O 设备的处理程序等功能。

BIOS 各功能模块的入口地址存放在中断向量表，可由 INT n 指令调用（可通过寄存器传递参数）

DOS 中断：INT 21H  
- 包含常用功能程序，实现文件管理、存储管理等
- 通过传入功能号参数选择功能
- 比 BIOS 中断功能齐全
- 进一步屏蔽了设备的物理特性和接口特性

## 外部中断

x86 CPU 为外部中断提供两个引脚  
- NMI：非屏蔽中断
  - NMI 引脚直接连接外设
- INTR：可屏蔽中断
  - INTR 引脚连接中断控制器，中断控制器连接外设

可编程中断控制器（PIC）：8259A

现代计算机中，APIC（Advanced PIC）被集成到南桥，CPU 中还有用于多核通信的 Local APIC。

可屏蔽中断的处理过程：  
- 外设向 CPU 发送中断请求信号
- CPU 完成先行指令，发出中断响应信号
- CPU 读取中断类型码
- 之后与内部中断相同：
- 压栈当前 CS 和 IP
- 清除中断标志 IF 和单步标志 TF
- 取 CS 和 IP 的新值，跳转到中断服务程序
- 在中断服务程序中，可以适当开启中断
- 中断服务程序结束后，恢复现场

中断嵌套：在中断服务程序中，如果开启了中断，且 CPU 收到优先级更高的中断请求，则 CPU 会响应新的中断请求，完成后再返回到原来的中断服务程序。

# 输入输出接口

模型机的输入输出：I/O 端口统一编址

I/O 接口的动机：  
- CPU 和外设的速度差异
- 外设处理的信息格式、接口信号多样
  - CPU 的最小操作单元是字节，但串行设备是一位一位工作的
  - CPU 处理数字信号（方波），但喇叭是模拟信号（正弦波）
  - CPU 的电压和外设设备不同
  - 如果让 CPU 处理外设，CPU 的设计会变得极其复杂
- I/O 接口电路处理以上问题

I/O 接口的功能：  
- 数据缓冲
  - 解决 CPU 和外设的速度差异：少次多量处理
- 提供联络信息
  - 协调与同步数据交换过程
  - 网卡：网线通不通？有没有冲突？
- 信号与信息格式的转换
  - 模/数、数/模转换，串/并、并/串转换，电平转换
- 设备选择
- 中断管理
- 可编程功能

I/O 接口的组成：  
- 数据输入寄存器
- 数据输出寄存器
- 状态寄存器
- 控制寄存器
- 中断控制逻辑

现代 PC 中的 I/O 接口：  
- 网卡
- 并口控制芯片
- 南桥
- 显卡

## I/O 端口及其编址

**I/O 端口**：I/O 接口内部的一组寄存器。每个 I/O 端口有自己的端口地址（端口号），供 CPU 访问

I/O 端口编址方式：  
- I/O 端口和存储器分开编址
  - I/O mapped I/O
  - x86
- I/O 端口和存储器统一编址
  - Memory mapped I/O
  - ARM、模型机、MIPS

x86 的 I/O 指令：  
- `IN AC, PORT`：从 I/O 端口读入数据到 AL 或 AX
- `OUT PORT, AC`：将 AL 或 AX 的数据输出到 I/O 端口
- 端口地址 PORT 范围为 0~255，更大的地址用**间接寻址**
  - `IN AC`：从 DX 指向的端口读入数据到 AL 或 AX
- x86 系统总线中有一个 M/IO 信号，指明是存储器访问还是 I/O 端口访问

分开编址：  
- 优点
  - I/O 端口不占用存储器地址
  - I/O 专用指令的编码更短（尤其地址码），指令执行更快
  - 程序可读性更强

统一编址：  
- 优点
  - 用功能齐全的 MOV 指令访问 I/O 端口
  - I/O 访问与存储器访问统一为一套控制逻辑，简化内部结构，减少 CPU 的引脚数目
- 缺点
  - 由于I/O端口占用了一部分存储器地址空间，因而使存储地址空间减小
  - 由于利用访问存储器的指令来进行I/O操作，指令的长度通常比单独 I/O 指令要长，因而指令的执行时间也较长

## I/O 控制方式

I/O 控制方式的分类：  
- 程序控制方式
- 中断控制方式
- 直接存储器访问（DMA）控制方式

### 程序控制方式

程序控制方式：  
- 无条件传送方式
  - 直接传输数据，不检查外设是否准备好等信息
  - 优点：控制程序简单
  - 缺点：只适合简单外设
  - LED 灯
- 程序查询传送方式
  - CPU 通过执行一段程序不断查询外设状态
  - 当外设就绪时，才进行数据传送
  - 优点：准确可靠
  - 缺点：查询外设状态占用大量 CPU 时间
- 两种程序控制方式共同的优点：
  - 对外设要求低
  - 操作流程清晰
- 共同的缺点：
  - CPU 负责数据传送，占用 CPU 资源

程序查询方式的数据输出：  
- CPU 执行 OUT 指令，将**控制字**写入接口的**控制寄存器**，设置其工作模式
- CPU 执行 OUT 指令，将数据写到接口的**输出缓冲寄存器**中
- 接口将数据发到**并行数据输出信号线**，将**输出准备好信号**置为有效
  - 亦可由 CPU 写控制字将输出准备好信号置为有效
- 外设一旦发现**输出准备好信号**有效，就从**并行数据输出信号线**读取数据，将**输出回答信号**置为有效
- 接口发现**输出回答信号**有效，就将**状态寄存器**中的**输出缓冲空状态位**置为有效
- 此过程中，CPU 反复从状态寄存器中读出**状态字**，并检查状态字中的**输出缓冲空位**是否有效，若有效，则可以开启下一个输出过程


程序查询方式的数据输入：  
- 系统初始化时，CPU 将**控制字**写入接口的**控制寄存器**，设置其工作模式
- 外设将数据发到**并行数据输入信号线**，将**输入准备好信号**置为有效
- 接口发现**输入准备好信号**有效后，从**并行数据输入信号线**接收数据，放入**输入缓冲寄存器**，并将**输入回答信号**置为有效，阻止外设继续发送数据
- 接口将**状态寄存器**中**输入缓冲满状态位**置为有效
- CPU 反复从状态寄存器中读出**状态字**，并检查状态字中的**输入缓冲满位**是否有效，若有效，则从输入缓冲寄存器中读出数据
- 接口将**输入回答信号**置为无效，允许外设继续发送数据

### 中断控制方式

中断控制方式的数据输入：  
- 外设和并行接口电路的交互和程序控制方式相同
- 系统初始化时，CPU 将**控制字**写入接口的**控制寄存器**，设置其工作模式
- 外设将数据发到**并行数据输入信号线**，将**输入准备好信号**置为有效
- 接口发现**输入准备好信号**有效后，从**并行数据输入信号线**接收数据，放入**输入缓冲寄存器**，并将**输入回答信号**置为有效，阻止外设继续发送数据
- 接口通过**中断控制逻辑**向 CPU 发出中断请求信号，并将**状态寄存器**中**输入缓冲满状态位**置为有效
- CPU 响应中断请求，执行中断服务程序：
  - 中断服务程序中，CPU 从状态寄存器中读出**状态字**，并检查状态字中的**输入缓冲满位**是否有效，若有效，则从输入缓冲寄存器中读出数据
- 接口将**输入回答信号**置为无效，允许外设继续发送数据


中断控制方式的数据输出：  
- CPU 执行 OUT 指令，将**控制字**写入接口的**控制寄存器**，设置其工作模式
- CPU 执行 OUT 指令，将数据写到接口的**输出缓冲寄存器**中
- 接口将数据发到**并行数据输出信号线**，将**输出准备好信号**置为有效
  - 亦可由 CPU 写控制字将输出准备好信号置为有效
- 外设一旦发现**输出准备好信号**有效，就从**并行数据输出信号线**读取数据，将**输出回答信号**置为有效
- 接口发现**输出回答信号**有效，通过**中断控制逻辑**向 CPU 发出中断请求信号，并将**状态寄存器**中的**输出缓冲空状态位**置为有效
- CPU 响应中断请求，执行中断服务程序：
  - 中断服务程序中，CPU 从状态寄存器中读出**状态字**，并检查状态字中的**输出缓冲空位**是否有效，若有效，则可以开启下一个输出过程

中断控制方式的特点：  
- 优点：
  - CPU 和外设并行工作
  - 外设拥有申请服务的主动权
  - 满足了外设的实时性要求
- 缺点：
  - 外设和存储器之间的数据传送仍然需要 CPU 参与，占用 CPU 资源
    - 数据经过 CPU 寄存器中转，过程冗长
  - 中断本身的上下文切换开销

### 直接存储器访问（DMA）

直接存储器访问（DMA）：  
- 数据传送不需要 CPU 参与
- 由专门硬件控制电路 **DMA 控制器**（**DMAC**）控制，外设和存储器直接交换数据
- 需要和程序控制方式或中断方式结合使用

DMAC 工作步骤：以外设到内存传送为例  
1. CPU 设置 DMAC 内部配置寄存器
   - 一般包括：
     - 源地址初始值及地址增减方式，例如设置为某 I/O 端口，传送时不变
     - 目的地址初始值及地址增减方式，例如设置为内存地址，传送时递增
     - 传送数据长度（根据需要设置）
2. DMAC 处于空闲等待状态
3. I/O 接口向 DMAC 发出 DMA 传送申请
4. DMAC 响应 I/O 接口的申请
5. DMAC 向 I/O 接口发起总线读传输
6. DMAC 向存储器发起总线写传输
7. DMAC 重复 5 和 6，直到传送完成
   - 通常此时 DMAC 会通过中断通知 CPU
8. 返回 2，等待下一个 DMA 传送请求

有的 I/O 接口自带 DMAC，如声卡、网卡、显卡


## 并行接口

并行接口电路：  
- 以前多是独立芯片，如 Intel 8255A
- 现在多包含在一块多功能芯片中
- 目前现在很少接出来接口了，被 USB 取代了
- 通过并行的 8 根数据线以及 2 根握手信号线与外设连接


串行与并行：  
- 串行传输线数量少，并行传输线数量多
- 系统总线是并行的，串行通信需要复杂的串/并转换，而并行通信不需要
- 同频率下，并行的数据传输率高，但串行频率更高
- 串行避免信号线的串扰，并行则需要考虑串扰问题

现在串行更多（USB、PATA -> SATA）：  
- 差分信号传输技术
  - 发送端在两根线上发振幅相同、相位相反的信号
  - 接收端比较两根线的电压差，得到原始信号
  - 干扰完全不会影响两根线的差值（两根线很近，受到的干扰相同，都上升 1V 差值不变）
  - 而且差分信号也不会干扰别人
  - 缺点：需要两根等长等宽紧密相邻的线，布线难度高

## 并口芯片应用实例

Intel 8255A 的外部接口：  
- 电源 Vcc 和地线 GND
- 与外设的连线：
  - 端口 A：$PA_7$~$PA_0$
  - 端口 B：$PB_7$~$PB_0$
  - 端口 C：$PC_7$~$PC_0$
- 与系统总线的连线：
  - 复位：RESET
  - 数据线：$D_7$~$D_0$
  - 地址线：$A_1$、$A_0$
  - 控制：CS、RD、WR

与外设的连线：  
- A 和 B 均为 8 位端口，可分别设定为输入端口或输出端口，端口 A 功能更丰富
- 端口 C 分为两个 4 位的端口，可分别设定为输入端口或输出端口
  - 通常传送 A 和 B 的握手信号

8255A 有 4 个端口（可编程寄存器），因此有 2 根地址线  
- 当 CS 有效时，根据两根地址线选中其中一个端口
- 00~11 对应端口 A、B、C、控制端口

8255A 有三种工作方式：  
- 方式 0：基本输入/输出方式
  - 单向输入/输出，无握手信号
- 方式 1：选通输入/输出方式
  - 单向输入/输出，端口 C 用于握手信号
- 方式 2：双向输入/输出方式
  - 双向输入/输出，端口 C 用于握手信号

8255A 初始化时，用 OUT 指令向控制端口发送控制字。

控制字：

- 最高位为 0：方式选择控制字
- 最高位为 1：端口 C 按位置 1/置 0 控制字

# 总线

## 总线的基本结构

总线（bus，汇流排）：在多个模块（设备或子系统）之间传送数据的公共通路，包括：  
- 传输信息的电路
- 管理信息传输的协议

计算机中的总线：  
- 片总线/器件级总线/片上总线：CPU 内部的总线
- 内总线/系统总线/板级总线：主板上各设备之间通信
- 通信总线：计算机和外部控制设备（仪表、键盘）之间通信

总线模块：  
- 总线主模块（bus master）
  - 有总线控制能力，获得总线控制权后可以**主动发起总线传输**
  - CPU、DMA 控制器
- 总线从模块（bus slave）
  - 无总线控制能力，不能主动发起总线传输，只能被动响应总线主模块的请求
  - 存储器

总线译码器（bus decoder）：  
- 根据当前控制总线的总线主模块发出的地址信号，选择本次传输目标的总线从模块
- 从模块可以无视译码器的选中信号，总是响应总线主模块的请求
- 例如，假设总线地址空间 16 位宽，以 4KB 为单位划分从模块的地址空间
  - 从模块 1 的地址空间是 `0000H`~`0FFFH`，从模块 2 的是 `1000H`~`1FFFH`
- 则译码器只需要看地址信号的高 4 位，确定其输出的 `sel1`、`sel2`、`sel3` 等信号的值
  - 如果地址信号是 `1234H`，则高 4 位是 `1H`，`sel2` 等于 1，其余为 0
- 从模块可能只看得到低 12 位——只关心自己内部的地址空间

总线仲裁器（bus arbiter）：  
- 当多个主模块同时请求总线控制权时，仲裁器决定哪个主模块获得总线控制权
- 仲裁策略：静态、round-robin
- 接受来自不同主模块的 `req` 信号和总线的 `bus_busy` 信号，向不同主模块发送 `gnt` 响应信号。

总线内部有若干多选器：  
- 接收仲裁器和各主模块的信号，根据仲裁器的控制信号选择其中一个主模块的信号，输出给各从模块。
- 接受译码器和各从模块的信号，根据译码器的控制信号选择其中一个从模块的信号，输出给各主模块。
  - 主模块自己知道该不该收，不像从模块需要译码器信号

总线设计理念：  
- 可以乱收多收
- 不准乱发：乱发也没用，仲裁器不会选

为了同步更慢的设备，可以通过总线桥接器（bus bridge），再连接一条更慢更小的总线。

## 总线协议

总线标准的两种产生途径：
- 事实标准：各厂家逐渐形成的标准
- 国际标准

总线标准的内容：  
- 机械特性：模块插件的机械尺寸，总线插头、插座的规格及位置等
- 电气特性：总线信号的逻辑电平、噪声容限及负载能力等
- 功能特性：各总线信号的名称及功能定义
- 规程特性：各总线信号的动作过程及时序关系进行说明

各种总线的历史（不考）

PCI-E：  
- 串行，高频率获得高性能
- 全双工
- 点对点连接
- PCI-E x16：16 个串行通道（每个串行通道之间不需要同步，因为传输的是无关的信号）

PCI-E 带宽和编码（不考）

## AHB 时序图

Advanced Microcontroller Bus Architecture（AMBA）：  
- 包含了 APB、AHB、AXI、ACE 等多种总线协议

AHB 时序图：  
- HCLK
- HADDR：主模块发出的地址信号
- Control：主模块发出的控制信号
- HWDATA：主模块发出的数据
- HREADY：从模块发出的准备好信号
- HRDATA：从模块发出的数据

AHB 由 ARM 提出，属于片上总线。

### 读写一个数据

读一个数据：  
1. Address phase：主模块发出 HADDR 和 Control 信号
2. Data phase：从模块发出 HRDATA 信号

时钟周期被从模块准备好读数据的时间决定

写一个数据：
1. Address phase：主模块发出 HADDR 和 Control 信号
2. Data phase：主模块发出 HWDATA 信号

写的地址、控制信号和数据信号是分开的，这是为了和读统一。此外，分开可以使得前一个读返回数据和写的发送地址、控制信号重叠，效率更高。

内存访问时，CPU 高速缓存通常会通过 burst 传输直接传输一整行。这种单个数据的读写通常由 x86 的 IN/OUT 指令完成发起。

### 主模块发起传输时，从模块未准备好

典型情况：CPU 读取内存数据。CPU 为总线主模块，内存控制器为总线从模块，需要等待数十到数百个时钟周期才能返回数据

读传输：
1. Address phase：主模块发出 HADDR 和 Control 信号
2. Data phase：从模块设置 HREADY 信号为低电平，插入若干等待周期。然后置 HREADY 信号为高电平，发出 HRDATA 信号

写传输：
1. Address phase：主模块发出 HADDR 和 Control 信号
2. Data phase：从模块设置 HREADY 信号为低电平，插入若干等待周期，此时主模块需要一直维持 HWDATA 信号，直到采样到 HREADY 信号为高电平

如果在 Address phase 结束时发现从模块未准备好，则主模块也需要一直维持 HADDR 和 Control 信号，直到采样到 HREADY 信号为高电平

插入等待周期使得总线不需要因为最慢的从模块（或从模块的最慢情况）而延长。


### 多个连续传输

三次连续的写：
1. 主模块发出地址 A 及控制信号
2. 主模块发出地址 B 及控制信号，以及数据 A
3. 传输 A 完成，主模块发出地址 C 及控制信号，以及数据 B
4. 主模块发现从模块 HREADY 信号为低电平，维持 HADDR、Control 和 HWDATA 信号
5. 主模块发现从模块 HREADY 信号为高电平，传输 B 完成。主模块发出数据 C
6. 传输 C 完成

不同传输的地址和数据在时间上重叠，充分利用地址总线和数据总线。

由于传输 B 的等待周期，传输 C 的地址阶段受其影响而扩展

### 大量连续数据传输

CPU 需要从内存读 128 位，但总线位宽只有 32 位

HTRANS 控制信号：  
- IDLE（00）：不传输数据
- BUSY（01）：burst 传输中，主模块没准备好
- NONSEQ（10）：一次 burst 传输的第一个传输，或一个非 burst 传输。地址和控制信号与先前传输无关
- SEQ（11）：一次 burst 传输的后续传输。地址和控制信号与先前传输有关

HBURST 控制信号：
- SINGLE（000）：单次传输
- INCR（001）：连续传输，地址递增
- WRAP4（010）：4 次连续 wrap 传输
- INCR4（011）：4 次连续传输，地址递增
- WRAP8（100）：8 次连续 wrap 传输
- INCR8（101）：8 次连续传输，地址递增
- WRAP16（110）：16 次连续 wrap 传输
- INCR16（111）：16 次连续传输，地址递增

示例：  
1. 主模块发出地址 0x38 及控制信号。HTRANS 为 NONSEQ，HBURST 为 INCR4。在整个传输过程中 HBURST 都将保持为 INCR4
2. 主模块发出地址 0x3c 及控制信号，以及 0x38 数据。HTRANS 为 SEQ，且此后传输过程中都保持为 SEQ
3. 主模块发现从模块 HREADY 信号为低电平，维持 HADDR、Control 和 HWDATA 信号
4. 主模块发现从模块 HREADY 信号为高电平，传输 0x38 完成。主模块发出地址 0x40 及控制信号，以及 0x3c 数据。HTRANS 为 SEQ
5. 如此重复，直到最后一个传输 0x44 完成，主模块发出 HTRANS 为 IDLE 的信号，传输结束

Burst 传输的应用：
- 内存访问时，CPU 高速缓存通常会通过 burst 传输直接传输一整行：行填充（读）、行替换（写）
- 外设和主存之间的大量连续数据传输
- 主存中不同区域之间的大量连续数据传输

I/O 操作不能通过 cache 做中介：  
- 连续写一百次 cache，只有最后一次有效
- I/O 操作需要保证每次写都是真正的写
- 内存是读不破坏的，I/O 设备可能是读破坏的
- CPU 执行 I/O 操作时，在总线发起单个的传输

ARM 的字（word）为 32 位，Intel 的字为 16 位。

关键字优先的传输： 
- CPU 读取 10018H，高速缓存不命中，向内存发起读请求读入 10010H 起的一整行：若顺序传输，则真正用到的 10018H 必须在第三个传输后才能得到，浪费两个总线时钟周期（而 CPU 时钟周期通常比总线时钟周期短很多！）
- WRAPn：传输操作的地址不连续，先发起优先级更高的传输。n 是连续传输的次数
- 地址回卷的边界：
  - WRAP4：在 16 的整数倍地址处回卷（0x38, 0x3c, 0x30, 0x34）
  - WRAP8：在 32 的整数倍地址处回卷（0x38, 0x3c, 0x20, 0x24, 0x28, 0x2c, 0x30, 0x34）

Burst 传输中，主模块没准备好：主模块设置 HTRANS 为 BUSY
- 下一个传输周期，如果是写，则主模块不用提供写数据（也提供不了），并将地址信号维持；如果是读，则从模块不用提供读数据

需要传输的数据宽度比数据总线的宽度小：主模块设置 HSIZE 信号
- HSIZE 信号有三位
  - 000：8 位（字节）
  - 001：16 位（半字，x86 中的字是 16 位）
  - 010：32 位（字，注意 ARM 字是 32 位）
  - 011：64 位
  - 100：128 位（四字）
  - 101：256 位（八字）
  - 110：512 位
  - 111：1024 位
- 写：数据线有 32 位，当主模块发 16 位数据时，高 16 位和低 16 位都填充该数据，以宽容从模块的设计（从模块拿哪个都行）
- 读：当主模块收 16 位数据时，它应根据传输地址收数据线正确位置的数据。例如，若读 0x30 地址的 16 位数据，则主模块应读取数据线低 16 位；若读 0x32 地址的 16 位数据，则主模块应读取数据线高 16 位
- 设计上对主模块要求更高

# 中断控制器及定时器

MIPS 架构中的异常处理硬件：
- EPC：异常程序计数器（Exception Program Counter），存放引起异常指令的地址（当前 PC - 4）
- Cause：存放产生异常的原因
- PC 更新逻辑可以将 PC 更新为异常处理程序入口地址
  - 所有异常处理程序都在一起
- 跳转到异常处理程序时，清除译码阶段和执行阶段的指令

## 中断控制器

外部中断的需求：
- 优先级：多个中断请求同时发生，如何确定次序
- 屏蔽：屏蔽指定 I/O 接口设备的中断请求

确定外部中断优先级：
- 软件查询
  - 所有外部中断请求信号或起来，产生一个总中断请求信号发给 CPU
  - 中断服务程序的开始部分依次查询各个设备是否有中断请求
  - 先查的设备优先级高（一般是速度较快或实时性高的设备）
- 硬件中断优先级编码电路
  - 菊花链
  - 中断请求：高电平有效
  - 中断响应：低电平有效
- 可编程中断控制器
  - 典型：Intel 8259A
  - 实现中断优先级仲裁
  - 为 CPU 提供中断类型码
  - 提供中断屏蔽功能
  - 中断控制器在 IBM PC/AT 中的连接结构
    - IR0：日时钟定时器
    - IR1：键盘
    - IR2：保留，可以级联另一个中断控制器
    - IR3：串口 1
    - IR4：串口 2
    - IR5：硬磁盘
    - IR6：软磁盘
    - IR7：打印机
  - 高级可编程中断控制器（APIC）
  - 现在已经集成在南桥，同时多核 CPU 的每个核也有 local APIC，用于多核同步

## 定时器

计数：主要是指对外部事件发生的次数进行计量

定时
- 主要是指产生一段准确的时延
- 定时是对固定的时间单位进行计数
- 定时本质上也是计数，定时是以计数为基础的

计数/定时的主要用途：
- 计算机系统
  - 系统的日时钟
  - 接口电路的工作节拍、信息传送的同步信号
  - 同步：保证在确定时刻完成规定动作
  - 提供时间基准，供操作系统切换进程使用
- 工业测控系统
  - 计数：对零件和产品的计数、对车流量的统计
  - 定时：定时采样、定时检测、定时扫描
- 声音/音乐
  - 由计数/定时的基本功能派生而来

可编程计数器/定时器
- 软硬件相结合的定时计数方法，采用专用的定时电路，其定时值通过软件进行控制
- 特点：**功能灵活，使用方便**
- 早期典型：Intel 8253，现在已经集成在主板

Intel 8253：
- 3 个计数器/定时器通道，操作完全独立
- 每个计数器有两个输入和一个输出
  - CLK：时钟输入信号，作为计数脉冲，可以是非周期性脉冲，也可以是频率精确的周期性脉冲
  - GATE：门控输入信号，对计数过程进行控制，具体作用视工作方式而定
  - OUT：计数输出信号，“计数到零/定时时间到”输出，输出信号形式视工作方式而定
- 8253 内部寄存器的寻址
  - $\bar{RD}$ 和 $\bar{WR}$ 信号控制是写还是读
  - $A_0$ 和 $A_1$ 组合寻址
    - 写操作只会写计数初值寄存器（地址 0、1、2）或控制字寄存器（地址 3）
    - 读操作只会读输出锁存器（地址 0、1、2）
- **方式 0：计数到零产生中断请求**
  - 写入控制字 CW，OUT 从高电平变为低电平
  - 写入计数初值 N 后，经一个 CLK 下降沿，开始计数（从 N 开始，到 0 结束）
  - 每输入一个 CLK 脉冲，CE 减 1
  - 在 N + 1 个 CLK 下降沿后，CE 变为 0，OUT 从低电平变为高电平
  - 计数过程中通过 GATE 信号控制计数：
    - GATE 为低电平时，暂停计数
    - GATE 为高电平时，继续计数
  - 特点：
    - 软件启动
    - 单次计数，不能自动重复
    - 计数结束时 OUT 变为高电平，可作为中断请求信号
    - 又称 Interrupt on Terminal Count（ITC）方式
  - 用途
    - 事件技术：零件、产品计数
    - 倒计时
- 方式 2：分频器
  - 写入控制字后，OUT 从低电平变为高电平
  - 写入计数初值 N 后，经一个 CLK 下降沿，开始计数（从 N 开始，到 1 结束）
  - 在 $N$ 个 CLK 下降沿后，CE 变为 1，OUT 从高电平变为低电平
  - 下一个 CLK 下降沿，CE 恢复为 N，OUT 变回高电平，重复以上过程
  - 特点：
    - 软件启动
    - 自动重复
    - 周期性输出
  - 用途：
    - 脉冲速率发生器，类似于 Divided-by-N Counter，每 $N$ 次 CLK 下降沿输出一个脉冲
    - 产生 DRAM 定时刷新信号
    - 若刷新频率为 64 Khz，输入时钟频率为 1.19318 Mhz，则 N = 1.19318 / 64 = 18
- 方式 3：方波发生器
  - 写入控制字后，OUT 从低电平变为高电平
  - 写入计数初值 N 后，经一个 CLK 下降沿，不断生成方波
  - 若 $N$ 为偶数，则方波对称，周期为 $N$，前 $N/2$ 个 CLK 下降沿为高电平，后 $N/2$ 个 CLK 下降沿为低电平
  - 若 $N$ 为奇数，则方波不对称，前 $(N + 1)/2$ 个 CLK 下降沿为高电平，后 $(N - 1)/2$ 个 CLK 下降沿为低电平
  - 特点：
    - 软件启动
    - 自动重复
  - 用途：
    - 产生系统时钟
    - 扬声器发声控制

## 南北桥架构的演变

最开始：
- CPU 只包含 CPU 核心和高速缓存
- 北桥：
  - PCIe 控制器：连接 PCIe 显卡，PCIe 显卡连接显示器
  - 主存控制器：连接主存
  - 集成显卡：连接显示器
- 南桥：
  - I/O 控制器：连接硬盘、网络、键盘、PCI 卡等

后来：
1. 主存控制器集成到 CPU 中
2. PCIe 控制器和集成显卡的图形部件也集成到 CPU 中，北桥消失。南桥变为 PCH（Platform Controller Hub），包含集成显卡的显示部件和 I/O 控制器
3. System-on-a-Chip（SoC）：全都继承到一个芯片里

# 流水线优化技术

80486 的五级流水线涉及：  
- 取指
- 译码
- 地址生成
- 执行
- 回写

Pentium 的**超标量流水线**：**双发射**
- 取指
  - **取指不能并行**，因为**指令长度不一**
  - 多取一些字节，**译码部件找前两个指令**，双发射
- 译码
- 两条并行的地址生成
- 两条并行的执行
- 两条并行的回写

Pentium Pro:  
- **将复杂的 x86 指令拆分成简单的微指令**（微操作，$\text{\mu op}$）

Pentium 4：Hyper-Threading

转移指令：
- 无条件直接转移
- 无条件间接转移
- 条件直接转移

转移指令会改变指令流向，破坏流水线模式，分支预测错误对流水线吞吐量影响很大

转移指令对性能的影响：
- 转移指令所占比例（15%~25%）
- **转移指令损失**：一条转移指令使流水线停顿的周期数
- 上两者相乘即流水线因转移指令损失的总周期数
- **流水线越深，超标量数越大，转移指令损失越大**

转移指令被执行并确实转移时的开销：转移开销
- 将按顺序预取的指令废除（排空流水线）
- 从转移目标地址重新取指

**延迟转移技术**：  
- 编译过程中，通过编译器调度，在转移指令之后插入一条或几条无关指令（转移不转移都会被执行的指令）
- 编译器成为处理器 specific 的

转移预测技术  
- 预测**转移条件是否成立**
- 预测**转移目标地址**

转移条件判定：  
- **硬件固定预测不转移**
  - 不特殊处理，顺序取下一条指令，错误就清空流水线
- **编译制导的预测**
  - 在转移指令编码增加一位，存放编译器给出的预测建议
  - 优点：软件可根据指令类型和历史信息，对不同指令进行不同的预测
  - 缺点：需要软件支持，需要修改 ISA，不适应多变的执行环境
- **基于偏移的预测**
  - 如果转移指令地址和目标地址的相对偏移为负（向前转移），预测转移；否则预测不转移
  - Back Taken, Forward Not taken (BTFN)
- **基于历史信息的预测**
  - **当前主流**
  - 先前转移指令实际发生转移，就预测转移；否则预测不转移
  - Pentium 的两位历史信息转移预测器：
    - 初始状态为 11，每错一次递减 1，每对一次递增 1。只有 00 时才预测不转移
    - 偏向于预测转移发生

转移目标地址预测：  
- 转移目标缓存（Branch Target Buffer, BTB）
- 存放近期历史转移指令的
  - 转移目标地址
  - 转移条件成立与否
  - 转移指令地址
- 当转移指令被执行时，先在 BTB 中查找
  - 如果没找到，在 BTB 中添加表项，固定预测不转移
  - 如果找到，即 BTB 命中，则检查 BTB 表项。若表项中转移条件成立，则将转移目标地址送到 PC
  - 之后，对比转移指令实际执行的结果，如果转移条件或转移目标地址与 BTB 中的表项不符，则更新 BTB 表项
- BTB 表项比较的时机：
  - 取指时：
    - 优点：尽早获得转移目标地址
    - 缺点：每条指令均需访问 BTB，开销大
  - 译码后：
    - 优点：只有转移指令需要访问 BTB
    - 缺点：较晚获得转移目标地址
  - UltraSPARC III：预译码（只检查是否为转移指令）完成后，取指的同时

## 返回地址栈

`ret` 指令需要专用的预测部件：返回地址栈 Return Address Stack（RAS）

`ret` 指令：
- 优点：
  - 均为无条件转移，无需判定转移条件
  - 转移目标地址确定
  - `call` 和 `ret` 成对出现
- 缺点
  - `ret` 指令的转移目标地址不定
  - 转移目标地址在存储器中，流水线晚期才能访问，且访问时间较长
- BTB 适合循环分支的预测，不适合 `ret` 指令的预测

返回地址栈：
- 每当执行 `call` 时，将 `call` 的下一条指令的地址（返回地址）压入返回地址栈中
- 每当执行 `ret` 时，从返回地址栈中弹出一个地址，作为转移目标地址


# 计算机系统先进技术

## 片上互连结构

芯片的集成度越来越高，包括很多处理单元和存储单元（System-on-Chip，SoC）

片上总线难以满足需求：主模块 1 长时间等待从模块 2 返回读数据时，其他本可以并行的传输被阻塞（主模块 2 到从模块 3 的读、主模块 1 到从模块 4 的写）

AXI（Advanced eXtensible Interface）：
- burst transactions 只需要发射首地址
- 读和写频道分离
- 发射多个 outstanding（未决）地址
- 乱序的 transaction completion
- 支持不对齐的数据

片商通信结构的问题：  
- 吞吐率：并行
- 功耗：一个发，所有都收到
- 信号延迟：被最慢的主从通信拖累
- 全局同步：全局异步、局部同步
- 信号完整性：数据纠错、重传

## 内存技术

见 [DRAM](#dram)

## 中断机制

PCI 总线的中断信号属于边带信号（sideband signal），使用方法未被总线规范明确。

问题：  
- PCI 设备使用 DMA 方式写存储器时，会在最后一个数据离开发送 FIFO 时认为 DMA 写操作完成，通过 INTx 信号向 CPU 发出中断请求。然而 CPU 收到中断信号时，PCI 设备可能还没将数据写入存储器

解决方法：  
- PCI 保证存储器写操作完成，再发中断
  - 读刷新：向 DMA 写的数据区域再发一个读请求，当读请求完成后，向处理器发中断
  - 读会保证前面的写操作完成
  - 硬件开销大
- 中断服务程序读刷新
  - 利用 PCI 总线的传送序规则，保证数据写入存储器
  - 大多数处理器系统采用此方法
  - 操作：中断服务程序先读 PCI 设备的中断状态寄存器，判断中断产生原因，再操作数据
  - 不影响效率，因为大多数情况下本来就需要执行上述操作

MSI（Message Signaled Interrupts）：  
- 当设备向一个特殊地址写入时，会向 CPU 发一个 MSI 中断
- PCI 2.2 定义，PCI 3.0 扩展到 MSI-X
- PCI 总线中必须支持 INTx 引脚中断，MSI 机制是可选的
- PCIe 总线中必须支持 MSI 机制

MSI 机制的特点：
- 优点
  - 解决了多个设备共享中断信号的问题
  - 解决了每个设备只支持一个中断的问题
  - 异步中断的数据完整性被保证：产生中断的写不会越过数据的写，中断服务程序可确信所有数据已经到达内存
- 缺点：占用总线带宽

## 芯片制造和封装

晶圆（wafer）=> 裸片（die）=> 芯片（Chip）

1. 从二氧化硅提纯单晶硅锭（ingot）
2. 横向切割硅锭，抛光，得到晶圆
  - 晶圆越大越好：一方面边角料占比更少，另一方面生产时并行性更高
3. 光刻胶
4. 光刻：掩模（mask）上印着电路图案，光刻胶层通过掩模被紫外线照射，变得可溶
5. 溶解光刻胶：曝光在紫外线下的光刻胶被溶解，留下电路图案
6. 蚀刻：溶解掉暴露出来的晶圆，剩下的光刻胶保护不该被溶解的晶圆
7. 去除光刻胶
8. 再次涂覆光刻胶
9. 离子注入
10. 去除光刻胶

微连接技术：  
- 引线缝合（wire bonding）：将芯片上的金属焊线与封装引脚连接
- 倒装片

## 指令系统

几张大表的考点：
- 总体上趋势（寻址方式比较多、指令长度变长...）
- 不考具体型号的具体特征

ARM（Advanced RISC Machine）指令系统：
- 寄存器编号位域只有 4 位：只能寻址 16 个通用寄存器
- 功能位域的位置不统一，译码逻辑不便
- 所有 ARM 指令都带 4 位条件码，都可以条件执行
  - 挤压了寄存器位域的空间
  - `if (a > b) a++; else b++;` 编译成 ARM 后：
  - `CMP R0, R1; ADDHI R0, R0, #1; ADDLS R1, R1, #1;`
  - 不需要分支预测！

RISC-V（Reduced Instruction Set Computer - Five）指令系统：
- R 型：寄存器-寄存器
- I 型：短立即数和访存 load
- S 型：访存 store
- B 型：条件跳转
- U 型：长立即数
- J 型：无条件跳转
- 格式比 MIPS 多，编码没有 MIPS 那么规整

x86 指令系统的发展：
- 兼容性：每款处理器支持前辈的所有指令、寄存器和操作方式
- 80286：新增 8 个通用寄存器压栈指令 PUSHA；立即数的移位次数 `SHL AX, 31`
- 80386：指令扩展为 32 位；新增符号扩展传送指令 `MOVSX`；条件转移的位移量可以是 32 位
- 80486：新增比较并交换指令 `CMPXCHG`
- Pentium：新增处理器特征识别 `CPUID` 指令; `MOV` 的源操作数可以使用控制寄存器 CR4
- Pentium MMX：新增 MMX 指令（MultiMedia eXtensions），用于多媒体处理。拥有 57 条 SIMD 指令，不能与浮点数操作同时进行
- Pentium III：SSE（Streaming SIMD Extensions）指令集，包含 70 条 SIMD 指令，支持浮点数操作
- Pentium 4 (Willamette 核心)：SSE2
- Pentium 4 (Prescott 核心)：SSE3
- 以上不需要记忆，了解趋势在往 SIMD 方向发展即可

### 并行结构

典型并行结构（Flynn 分类法）：
- Single Instruction Single Data (SISD)：单核
- Single Instruction Multiple Data (SIMD)：向量处理器
- Multiple Instruction Single Data (MISD)：不常见
- Multiple Instruction Multiple Data (MIMD)：多核

x86 的 SIMD
- MMX
- SSE ~ SSE4
- AVX ~ AVX2 (Advanced Vector Extensions)

MMX 指令：
- 数据类型
  - Packed byte：8 个 8 位整数打包进一个 64 位寄存器
  - Packed word：4 个 16 位整数打包进一个 64 位寄存器
  - Packed doubleword：2 个 32 位整数打包进一个 64 位寄存器
  - Packed quadword：1 个 64 位整数打包进一个 64 位寄存器
- 8 个 MMX 寄存器：`MM0` ~ `MM7`
  - 复用了浮点寄存器，MMX 操作不能与浮点数操作同时进行
- MMX 指令
  - P 开头的指令：Packed 操作
  - EMMS：清空 MMX 状态，切换到浮点数操作，开销大
  - 饱和运算：
    - 默认指令是回卷（wrap-around）的，例如 `0xf000 + 0x3000 = 0x2000`
    - 饱和运算指令：`0xf000 + 0x3000 = 0xffff`
  - 具体指令不需要记忆，能看懂即可

### GPU

计算机图形学：如何在计算机中表示图形、以及利用计算机进行图形的计算、处理和显示的相关原理与算法

图形加速器：
- 3D 图形处理可以划分成几何处理和光栅化处理两个阶段
  - 几何处理：坐标变换、裁剪、光照计算等
  - 光栅化处理：扫描转换、Z-Buffer计算、纹理映射等
- 实时 3D 图形生成计算量巨大，通用 CPU 难以胜任，需要采用专用硬件加速

NVIDIA 可编程 GPU：GeForce 7800

NVIDIA 统一渲染管线 GPU：GeForce 8800

趋势：专门图形加速 => 通用的 GPGPU（General-Purpose GPU）计算

CPU 和 GPU 的区别：
- CPU 面向延迟优化
  - 强大的 ALU
  - 缓存容量大
  - 复杂的控制逻辑（分支预测、数据前递）
- GPU 面向吞吐量优化
  - 大量、高延迟、低功耗的 ALU
  - 缓存容量小
  - 简单的控制逻辑（不需要分支预测、数据前递）


# 考试范围

ENIAC 设计者 指标 不需要记

EDVAC，冯诺依曼结构特点要记。第一台冯诺依曼 EDSAC

第一台商用的 UNIVAC。其他的计算机不用记

计算机类别：超级、大型、小型、微型。代表型号不用记

微型计算机的主要形态：笔记本 台式 平板 手机 服务器 嵌入式

冯诺依曼结构内容 思想 五个部分

模型机执行指令的四个步骤

**南北桥架构演变**，SoC

x86 体系结构：CPU 代表型号字长/架构，扩展了什么解决了什么

标志位：和跳转指令有关系的，基本的

指令指针寄存器，怎么扩展的。

两种晶体管，功能。与或非门怎么搭

慕课部分范围和期中考试一样

注意控制信号的那个表

控制器的控制信号的那个表 LOAD T1

指令和数据 cache 合起来和分开，多级的优劣

BIOS 中断不需要记，但是会给出来表，需要看得懂表

课堂 差分信号前后的具体接口不用掌握

总线的那个图。AHB（别的不用会）时序图

MIPS 处理异常的部件

优先级判定