- [课程概述](#课程概述)
- [计算机基本结构](#计算机基本结构)
  - [电子计算机的兴起](#电子计算机的兴起)
  - [模型机](#模型机)
  - [现代计算机](#现代计算机)
- [指令集](#指令集)
  - [x86 体系结构](#x86-体系结构)
    - [16 位时代：x86-16（IA-16）](#16-位时代x86-16ia-16)
    - [32 位时代：IA-32](#32-位时代ia-32)
    - [64 位时代：x86-64](#64-位时代x86-64)
  - [x86 指令简介](#x86-指令简介)
  - [MIPS 体系结构](#mips-体系结构)
- [算术逻辑单元](#算术逻辑单元)
  - [门电路](#门电路)
  - [寄存器](#寄存器)
  - [加法器](#加法器)
- [乘法器与除法器](#乘法器与除法器)
  - [乘法器](#乘法器)
  - [除法器](#除法器)
- [单周期处理器](#单周期处理器)
  - [数据通路](#数据通路)
  - [控制信号](#控制信号)
  - [控制信号的集成](#控制信号的集成)
- [控制器的基本原理](#控制器的基本原理)
  - [硬布线控制器](#硬布线控制器)
  - [微程序控制器](#微程序控制器)
- [流水线处理器](#流水线处理器)
  - [流水线冒险](#流水线冒险)
- [存储层次结构](#存储层次结构)
  - [DRAM](#dram)
  - [SRAM](#sram)
  - [Cache](#cache)
  - [单位](#单位)
- [中断和异常](#中断和异常)
  - [内部中断](#内部中断)
  - [外部中断](#外部中断)
- [输入输出接口](#输入输出接口)
  - [I/O 端口及其编址](#io-端口及其编址)
  - [I/O 控制方式](#io-控制方式)
    - [程序控制方式](#程序控制方式)
    - [中断控制方式](#中断控制方式)
    - [直接存储器访问（DMA）](#直接存储器访问dma)
  - [并行接口](#并行接口)
  - [并口芯片应用实例](#并口芯片应用实例)
- [总线](#总线)
  - [总线的基本结构](#总线的基本结构)
  - [常见总线协议](#常见总线协议)

# 课程概述

# 计算机基本结构

## 电子计算机的兴起

**现代电子计算机之父**：冯·诺依曼

ENIAC（Electronic Numerical Integrator and Computer，电子数字积分器和计算机）：  
- 首台**通用**电子计算机
- 1946年2月14日
- 1943年开始设计
- 宾夕法尼亚大学
- 约翰·莫克利（首席顾问）、约翰·埃克特（首席工程师）、约翰·冯·诺依曼

ABC（Atanasoff-Berry Computer，阿塔纳索夫-贝里计算机）：  
- 比 ENIAC 早，1939年完成
- 首台计算机

冯诺依曼提出**存储程序**概念，在《关于 EDVAC 的报告草案》提出了冯诺依曼结构。

EDVAC（Electronic Discrete Variable Automatic Computer，电子离散变量自动计算机）：  
- 1949年8月交付
- 1951年开始运行
- 冯诺依曼结构
- 存储程序
- 指令和数据采用二进制，简化了逻辑线路
- 五个基本部分：
  - 运算器 CA
  - 控制器 CC
  - 存储器 M
  - 输入设备 I
  - 输出设备 O
  - （外部记录介质 R）
- 但第一台冯诺依曼结构计算机是 EDSAC（1949年正式运行）

UNIVAC（UNIVersal Automatic Computer，通用自动计算机）：1951 年交付，1952 年统计选票一战成名，开启了商用计算机时代。

其他经典计算机：  
- 1964 年大型计算机 IBM S/360
- 1964 年超级计算机 CDC 6600
- 1965 年小型计算机 PDP-8
- 1975 年微型计算机 Altair 8800
- 1977 年微型计算机 Apple II
- 1981 年微型计算机 IBM PC 5150

## 模型机

存储器（主存）：  
- 控制总线连接控制逻辑和地址译码器
- 地址总线连接 Memory Address Register（MAR）
- 数据总线连接 Memory Data Register（MDR）

控制器：  
- 指令寄存器（IR）：存当前指令
- 程序计数器（PC）：存下一条指令的地址
- MAR
- MDR
- 指令译码部件
- 控制电路

运算器：  
- ALU：算术运算和逻辑运算
- 通用寄存器
- 状态寄存器

CPU：控制器和运算器

总线：  
- 内部总线：CPU 内部
- 控制总线：控制器的控制电路和存储器控制逻辑
- 地址总线：存储器 MAR 和控制器 MAR
- 数据总线：存储器 MDR 和控制器 MDR

执行指令的四个步骤：取指、译码、执行、回写

输入输出：就像另一个存储器

## 现代计算机

现代计算机结构：  
- CPU
- 北桥：PCIe 控制器、主存控制器、集显
- 南桥：I/O 控制器（硬盘、USB、网卡、PCI 卡、...）

后来，主存控制器、PCIe 控制器、集显（核显）都集成在 CPU 里了。主板只剩下一个 PCH 管 I/O。

再后来发展出了 SOC（System on Chip，片上系统）。

摩尔定律（常见描述）：价格不变，每 18 个月，集成电路上可容纳的晶体管数目翻一番。


# 指令集

简单的指令集示例：  
- 运算类：`ADD R, M`
- 传送类：`LOAD R, M`、`STORE M, R`
- 转移类：`JMP L`
- 格式：4 位操作码 + 4 位寄存器号 + 8 位地址

## x86 体系结构

### 16 位时代：x86-16（IA-16）

Intel 8086：  
- 1978 年发布
- 16 位通用寄存器，也支持 8 位数据
- 16 根数据线
- 20 根地址线，寻址空间 1MB
- 段 + 偏移量寻址
- 代表计算机 IBM 5150（用的是简化版的 8088）

8086 的寄存器模型：  
- 通用寄存器
  - 4 个数据寄存器：AX、BX、CX、DX
    - 每个可分为两个 8 位寄存器使用：AL/AH、BL/BH、CL/CH、DL/DH
  - 指针和变址寄存器：SP、BP、SI、DI
    - 每个对应的低 8 位寄存器：SPL、BPL、SIL、DIL
- 指令指针寄存器：IP
- 标志寄存器：FLAGS
  - 标志位有：
    - 状态标志位：CF（进位）、ZF（零）、SF（符号）、OF（溢出）、PF（奇偶）、AF（辅助进位）
    - 控制标志位：DF（方向）、IF（中断）、TF（跟踪）
- 段寄存器：CS、DS、SS、ES
  - CS：代码段寄存器，指向当前执行的代码段
  - DS：数据段寄存器，指向当前数据段
  - SS：堆栈段寄存器，指向当前堆栈段
  - ES：附加数据段寄存器

8086 的寻址方式：  
- 指令指针寄存器（IP）保存**下一条指令的地址**（即程序计数器）
- 程序员不能直接操作 IP
- 物理地址 = 段寄存器 * 16 + IP
- 1MB 的存储空间分为许多 64KB 的逻辑段，它们可以重叠
- 指令的操作数默认存放在数据段（DS）中，指令地址默认存放在代码段（CS）中，**BP 默认使用 SS 段**

Intel 80286：  
- 24 位地址总线，寻址空间 16MB
- 保护模式，但有缺陷（每个段只有 64KB）
- 实模式（兼容 8086）
- 所有 x86 处理器在加电启动时都首先进入实模式，系统初始化完成后再进入保护模式

### 32 位时代：IA-32

Intel 80386：  
- 8086 系列的首款 32 位处理器
- 32 位通用寄存器
- 32 位地址总线，寻址空间 4GB
- 改进的保护模式：段可达 4GB
  - 保护模式是主要工作模式，支持多任务、特权级、特权指令、访问权限、4GB 物理地址空间、虚拟内存
- 虚拟 8086 模式，可以模拟多个 8086 处理器
  - 寻址空间 1MB，段 + 偏移寻址（与实模式相同）
  - 对中断和异常的处理不同于实模式
  - 用于兼容 8086 系统

IA-32 对寄存器模型的扩展：  
- 通用寄存器（AX -> EAX）
- 指令指针寄存器（IP -> EIP）
- 标志寄存器（FLAGS -> EFLAGS）
- 段寄存器仍然都为 16 位，增加了 FS 和 GS 两个段寄存器

IA-32 的寻址方式：  
- 实模式：CS:IP
- 保护模式：
  - Global Descriptor Table（GDT）：全局描述符表，存放 8192 个段描述符
  - 每个描述符有 8 个字节：[段界限:2, 段基址:3, 权限:1, 其他:1, 段基址:1]
  - 16 位的 CS 寄存器恰好可以寻址 8192 * 8 字节 = 64KB 的 GDT
  - GDTR：GDT 寄存器，在 CPU 内部，存放 GDT 的基址和界限
  - 段基址由根据 GDTR 和 CS，查内存中的 GDT 得到
  - EIP：存放指令的段内偏移地址
- 物理地址 = 段基址 + 段内偏移地址

### 64 位时代：x86-64

AMD Opteron：  
- 首款 64 位 x86 处理器
- 兼容 IA-32，且不降低性能

x86-64 的运行模式：  
- 长模式：64 位 OS
  - 64 位模式：需要重新编译
  - 兼容模式：不需要重新编译
- 传统模式：都不需要重新编译
  - 保护模式：32 位或 16 位 OS
  - 虚拟 8086 模式：32 位或 16 位 OS
  - 实模式：16 位 OS

x86-64 对寄存器模型的扩展：  
- 通用寄存器（EAX -> RAX），并且增加了 R8-R15 寄存器
- 指令指针寄存器（EIP -> RIP）
- 标志寄存器（EFLAGS -> RFLAGS）
- 段寄存器没有变化，仍然为 6 个 16 位寄存器（CS、DS、SS、ES、FS、GS）

x86-64 半废除了 GDT 寻址，GDT 描述符表的基址和界限都为 0，段选择子也不再使用，通过分页寻址。

## x86 指令简介

主要分为传送类、运算类（又可分为算术、逻辑）、控制类、转移类。

字节（BYTE）为 8 位，字（WORD）为 16 位，双字（DWORD）为 32 位，四字（QWORD）为 64 位。

Intel 语法：`MOV WORD PTR [BX + SI*2 + 200H], 01H`  
- 目的在前，源在后
- `[]` 表示内存地址
- `WORD PTR` 表示操作数大小
- `H` 后缀表示十六进制，`D` 表示十进制，`B` 表示二进制，`O` 表示八进制

MOV：  
- 立即数不能为目的
- 不能在存储器之间传送，不能在段寄存器之间传送
- 不能将立即数传送到段寄存器
- CS 不能为目的

栈操作：PUSH、POP

LEA：将地址（而不是地址中的数据）传送到通用寄存器中

算术运算指令：  
- 目的不能是立即数或 CS
- 源和目的不能都是存储器
- 加法：ADD、ADC（目的 + 源 + CF）、INC
- 减法：SUB、SBB（目的 - 源 - CF）、DEC、CMP（DST - SRC，但不写回目的，只设置标志位）
- LEA 不是算术运算指令

逻辑运算指令：  
- 单操作数指令，操作数不能是立即数
- 双操作数指令，限制同 MOV 指令（目的不能是立即数或 CS，源和目的不能都是存储器也不能都是段寄存器，不能将立即数传送到段寄存器）
- NOT、AND
- SHL 逻辑左移（乘 $2^n$）
- SHR 逻辑右移，SAR 算术右移（除以 $2^n$）

转移指令：  
- 无条件转移：
  - 直接转移
    - `JMP SHORT LABEL`：LABEL 是地址，指令中存储的是 IP 到 LABEL 的 8 位偏移量，范围 -128 ~ 127。执行效果：`IP = IP + OFFSET`
    - `JMP NEAR PTR LABEL`：16 位偏移量，范围 -32768 ~ 32767（80386 支持 32 位偏移量）。执行效果：`IP = IP + OFFSET`
    - `JMP FAR PTR LABEL`：指令中存储的是 LABEL。执行效果：`CS = LABEL.CS`、`IP = LABEL.IP`
  - 间接转移：`JMP AX`、`JMP [BX]`、`JMP FAR PTR [SI]`（执行效果如 `IP = [SI]; CS = [SI + 2]`）
- 条件转移：
  - `C`：进位
  - `P`：奇偶
  - `Z`/`E`：零/相等
  - `S`：符号（负数）
  - `O`：溢出
  - `B(E)`、`A(E)`：小于 (等于)/大于 (等于)（无符号数）
  - `L(E)`、`G(E)`：小于 (等于)/大于 (等于)（有符号数）

处理器控制指令：  
- 标志操作
  - STC：设置进位标志 CF
  - CLC：清除进位标志 CF
  - CMC：取反进位标志 CF
- 外同步：HLT（停止 CPU）
- NOP

复杂 x86-64 指令举例：串操作指令  
- 串的基本单位：字节（BYTE）或字（WORD）
- 串长可达 64KB
- 五种串操作指令：MOVS、CMPS、SCAS、LODS、STOS，每种有 B 和 W 两种形式
- 三种重复前缀：REP、REPE、REPNE
- 隐含操作数：
  - 源串：**DS:SI**
  - 目的串：**ES:DI**
  - 串长：**CX**
  - 方向标志 **DF**（0：从低地址到高地址，1：从高地址到低地址）
- 传送过程中，SI、DI、CX 自动更新

**STD 和 CLD 指令**：设置和清除方向标志 DF。

## MIPS 体系结构

设计者：约翰·亨尼西

RISC（Reduced Instruction Set Computer，精简指令集计算机）、CISC（Complex Instruction Set Computer，复杂指令集计算机）

MIPS（Microprocessor without Interlocked Pipeline Stages，微处理器无互锁流水线阶段）：  
- 32 位定长指令
- 指令少、简单
- 仅 Load/Store 指令可访问内存
- 寻址简单
- 需要优秀的编译器

MIPS 的通用寄存器：`$0` - `$31`，分为若干组。

MIPS 的算术指令**不会自动设置标志位**。

MIPS 指令分为 R 型、I 型、J 型：  
- R 型：**ADD rd, rs, rt**、**JR rs**
  - 格式：[6 位 opcode, 5 位 rs, 5 位 rt, 5 位 rd, 5 位 shamt, 6 位 funct]
  - 所有 R 型指令的 opcode 都为 0，funct 决定具体指令
  - shamt：移位量，只对移位指令有效
  - rs、rt：源寄存器，rd：目的寄存器
- I 型：**ADDI rt, rs, imm**、**LW/SW rt, imm(rs)**、**BEQ rs, rt, offset**
  - 格式：[6 位 opcode, 5 位 rs, 5 位 rt, 16 位 immediate]
  - 与 R 型指令的区别：无 shamt、rd、funct
  - rs：源寄存器，rt：目的寄存器
  - immediate：立即数或地址偏移量
- J 型：**J addr**
  - 格式：[6 位 opcode, 26 位 address]
  - 无条件跳转
  - 目的地址 = {PC[31:28], address, 00}，即 **PC 的高 4 位 + 指令中的地址 + 00（低两位）**
  - MIPS 指令 32 位定长，所有指令地址都是 32 位（4 字节）对齐的，所以低两位都是 00

注意操作数顺序。

分支指令：  
- 条件分支：I 型
  - **BEQ/BNE rs, rt, offset**：如果 rs 和 rt 相等（不相等），则跳转到 **(PC + 4) + offset * 4**（注意操作数顺序）
- 无条件分支：J 型和 R 型
  - **J addr**：正负 256MB 跳转
  - **JR rs**：4GB 跳转

# 算术逻辑单元

`add $8, $9, $10` 的执行过程：  
- 取指：指令被取到 IR
- 译码：分析出指令类型、操作数，通过控制电路将加法命令传给 ALU，并将 `$9` 和 `$10` 的输出通过内部总线送到 ALU 的输入
- 执行：
  - ALU：执行加法操作，输出结果
  - 状态寄存器：设置状态标志位（如进位、零、符号、溢出等）
- 回写：将 ALU 的输出通过内部总线送到 `$8` 寄存器

算术运算指令 `add` 和 `sub` 可能引发溢出异常，带后缀 `u` 则不会。（溢出仅针对有符号数，有符号数的溢出会改变符号）  
- 有溢出不一定有进位：四位有符号数加法 `0b0011 + 0b0101 = 0b1000`
- 有进位不一定有溢出：四位无符号数加法 `0b1111 + 0b0001 = 0b0000`。当然也可以说无符号数的进位是无符号数的溢出，因为其结果不在表示范围内。
- 溢出：**最高位的进位输入不等于最高位的进位输出**
  - 溢出其实就是两个正数相加得到负数，或者两个负数相加得到正数
  - 只看最高位的全加器：
    - 若输入为两个正数，即 $A = B = 0$，则进位输出 $C_{out}$ 必然为 $0$；溢出当且仅当 $S = 1$，这要求 $C_{in} = 1$，满足 $C_{in} \neq C_{out}$
    - 若输入为两个负数，即 $A = B = 1$，则进位输出 $C_{out}$ 必然为 $1$；溢出当且仅当 $S = 0$，这要求 $C_{in} = 0$，满足 $C_{in} \neq C_{out}$
    - 即：溢出 $OF = C_{in} \oplus C_{out}$
- MIPS 没有 OF，有符号数溢出会引发异常。x86 不会引发异常，只会设置 OF。

I 型加法指令 `addi` 中的立即数是 16 位的，运算时会有符号扩展为 32 位。它也有不引发溢出异常的版本 `addiu`。

I 型逻辑运算指令 `andi`、`ori` 会对立即数进行零扩展。

## 门电路

晶体管（transistor）：现代集成电路通常用 Metal-Oxide-Semiconductor (MOS) 晶体管。  
- NMOS：当 Gate 为高电平时，Drain 和 Source 之间导通
  - NMOS 的 Gate 和 Source 之间没有小圆圈
- PMOS：当 Gate 为低电平时，Drain 和 Source 之间导通
  - PMOS 的 Gate 和 Source 之间有小圆圈。
- 助记：有小圆圈 -> 0 -> 不导通
- 两种晶体管组合成 Complementary MOS (CMOS) 晶体管
- 非门、与非门、与门、或门、异或门

## 寄存器

D 触发器（D Flip-Flop）：  
- 有存储能力的基本单元，由逻辑门构成
- 一个数据输入，一个时钟输入，一个输出
- 在时钟上升沿时，数据输入的值被锁存到输出中。在其他时刻，无论数据输入的值如何变化，输出都保持不变。
- **CLK-to-Q 延迟**：时钟上升沿到输出稳定的时间
- **Setup/Hold 时间**：数据输入在时钟上升沿前/后必须保持稳定的时间
  - 类比：显示器和相机
- 两个 D 触发器连起来，如果它们的时钟上升沿同时到达，则第一个触发器的输出不一定和第二个触发器的输出一致（可能是第一个触发器先前的输出）。
- 32 个 D 触发器并起来，就是一个 32 位寄存器

## 加法器

32 位与运算：并联 32 个与门。

将不同的 32 位运算单元再并联起来，再加上一个多选器选择运算结果，就可以实现 ALU。

半加器（Half Adder）：将两个一位二进制数相加，输出和与进位。  
- 注意到 $S = A + B = A \oplus B$，$C = A \cdot B$
- **半加器就是一个异或门和一个与门**

全加器（Full Adder）：将两个一位二进制数和一个进位相加，输出和与进位。  
- 注意到 
  - $S = A + B + C_{in} = (A \oplus B) \oplus C_{in}$
  - $C_{out} = A \cdot B + (A \oplus B) \cdot C_{in}$
- 由**两个半加器和一个或门**组成
- 把 32 个全加器串联起来，前一个的进位输出连接下一个的进位输入，就可以实现 32 位加法器

减法：转换为加法，$A - B = A + (-B) = A + \sim B + 1$  
- 在先前的 32 位加法器的第二个输入前添加一个 2-to-1 多选器
- 多选器的选择信号连接到最低位全加器的进位输入
- 如果多选器的选择信号为 $0$，则输出为 B，正常计算 $A + B$
- 如果多选器的选择信号为 $1$，则输出为 $\sim B$，计算减法 $A + \sim B + 1$，即 $A - B$（$1$ 来自最低位全加器的进位输入）

先前的加法器是行波进位加法器（Ripple Carry Adder），优点是简单，缺点是高位运算必须等待低位运算完成，延迟累积。  
- 记门延迟为 $T$，线延迟为零，则 32 位加法器的总延迟是 $3T + 31\times 2T = 65T$
  - 第 $i$ 位的进位信号依赖于第 $i-1$ 位的进位信号
  - 最低位的进位信号需要 $3T$
  - 更高位的进位信号需要 $2T$
- 需要提前计算进位信号

注意到 

$$C_{i+1} = (A_i \cdot B_i) + (A_i \cdot C_i) + (B_i \cdot C_i),$$

设  
- $G_i = A_i \cdot B_i$：生成（Generate）信号
- $P_i = A_i + B_i$：传播（Propagate）信号
- 这两个信号都由 $A$ 和 $B$ 决定，可以并行地计算出来，延迟不会累积

则 $$C_{i+1} = G_i + P_i \cdot C_i.$$

将这个式子不断迭代展开，可以得到 $C_{32}$ 的表达式：

$$
\begin{align*}
C_{32} &= G_{31} \\
&+ P_{31} \cdot G_{30} \\
&+ P_{31} \cdot P_{30} \cdot G_{29} \\
&\vdots \\
&+ P_{31} \cdot P_{30} \cdots P_1 \cdot G_0 \\
&+ P_{31} \cdots P_1 \cdot P_0 \cdot C_0
\end{align*}
$$

此时，进位信号的延迟固定为 $3T$：  
- 从 $A, B$ 计算 $G, P$ 为 $T$
- 展开式中的与运算延迟为 $T$
- 最后的或运算延迟为 $T$
- 每一位的运算都是并行的
- 缺点：电路复杂，位数越多越复杂
- 整个加法器的延迟为 $3T + T = 4T$（最高位进位信号通过最后一个全加器需要 $T$ 的延迟）

改进后的加法器称为超前进位加法器（Carry-Lookahead Adder, CLA）。32 位完全的超前进位加法器的延迟为 $4T$，但电路过于复杂难以实现，一般使用多个小 CLA 用行波进位的方式连接起来。  
- 设门延迟为 $0.02ns$，
- 32 位 RCA 的延迟为 $65 \times 0.02 = 1.3ns$
- 32 位 CLA 的延迟为 $4 \times 0.02 = 0.08ns$
- 4 级 CLA 的延迟为 $(3\times 0.02) \times 4 + 0.02 = 0.26ns$

# 乘法器与除法器

## 乘法器

只有零和一的十进制乘法和二进制乘法是一样的。

$N$ 位乘法器的组成：  
- **被乘数寄存器：$2N$ 位，带左移，低 $N$ 位存放被乘数**
- **乘数寄存器：$N$ 位，带右移**
- **积寄存器：$2N$ 位**
- **加法器：$2N$ 位**

$N$ 位乘法器的工作流程：  
1. 检查乘数寄存器最低位：
  - 如果为 $1$，将被乘数寄存器的值加到积寄存器中
  - 这个分支实际上可以取消：相当于先将乘数寄存器最低位和被乘数取与，然后再加到积寄存器中
2. 将被乘数寄存器的值左移一位
3. 将乘数寄存器的值右移一位
4. 如果这是第 $N$ 次循环，则结束；否则，返回第 1 步

时间优化（性能优化）：  
- 分析：加法、左移、右移都需要一个时钟周期，$N$ 位乘法将需要约 $3N$ 个时钟周期。
- 优化：**加法和移位操作并行**
  - 控制逻辑同时给出加法、左移和右移信号，在下一个时钟上升沿时，这三个操作就能并行完成
  - 被乘数寄存器的移位不会影响加法，因为移位操作完成时，被乘数寄存器的值已经发给输出端口了
- 现在，乘法器只需要 $N$ 个时钟周期。

空间优化（面积优化）：  
- 分析：
  - 被乘数寄存器有 $2N$ 位，但其中有效位只有 $N$ 位
  - 乘数寄存器有 $N$ 位，但每次右移后，它的有效位就减少一位
  - 乘积寄存器有 $2N$ 位，但每次加法的有效数字只有 $N$ 位
  - 加法器 $2N$ 位，但每次加法的有效数字只有 $N$ 位
- 优化：
  - 被乘数寄存器缩减为 $N$ 位，不左移
  - 乘积寄存器仍然为 $2N$ 位，初始有效数字存放在高 $N$ 位，随运算过程不断右移
  - 加法器也被自然缩减为 $N$ 位，每次取出乘积寄存器的高 $N$ 位与被乘数寄存器相加
  - 取消乘数寄存器，将乘数放在乘积寄存器的低 $N$ 位中，并将其与控制逻辑相连（随着右移，这低 $N$ 位的乘数逐渐被移出）
- 优化后乘法器的组成：
  - **被乘数寄存器：$N$ 位，存放被乘数**
  - **乘积寄存器：$2N$ 位，带右移，高 $N$ 位存放乘积，低 $N$ 位存放乘数**
  - **加法器：$N$ 位**


## 除法器

只有零和一的十进制除法和二进制除法是一样的。

带符号数的除法，余数和被除数符号相同。$-9 \div 4 = -2 ... -1$

$N$ 位除法器的组成：  
- **余数寄存器：$2N$ 位，存放 $2N$ 位的被除数（余数）**
- **除数寄存器：$2N$ 位，带右移，高 $N$ 位存放除数**
- **商寄存器：$N$ 位，带左移**
- ALU：$2N$ 位，支持加法和减法

$N$ 位除法器的工作流程：  
1. 余数 = 余数 - 除数
2. 检查余数最高位：
   - 若为 $1$，即余数为正，则商寄存器左移一位，并将最低位置 $1$
   - 若为 $0$，即余数为负，则将除数加回余数（回滚），商寄存器左移一位
3. 除数寄存器右移一位
4. 如果这是第 $N$ 次循环，则结束；否则，返回第 1 步

空间优化（面积优化）：  
- 分析：
  - 除数寄存器有 $2N$ 位，但其中有效位只有 $N$ 位
  - 商寄存器初始时空，随着运算过程逐渐填满
  - 余数寄存器初始时满，随着运算过程逐渐清空
- 优化：
  - 除数寄存器缩减为 $N$ 位，不右移
  - 取消商寄存器，商从低位逐位移入余数寄存器
  - ALU 也被自然缩减为 $N$ 位，每次取出余数寄存器的高 $N$ 位与除数寄存器做加减法
  - 运算完成后，余数寄存器的高 $N$ 位就是余数，低 $N$ 位就是商（**余数寄存器还需要支持右移，从而取出余数**）
- 优化后除法器的组成：
  - **余数寄存器：$2N$ 位，带左移、右移，存放余数和商**
  - **除数寄存器：$N$ 位**
  - ALU：$N$ 位，支持加法和减法

这种除法器不容易做时间优化了，它不像乘法器那样可以并行地做加法和移位。


# 单周期处理器

简化 MIPS 指令集：  
- 无符号加减法：
  - `addu rd, rs, rt`
  - `subu rd, rs, rt`
- 立即数的逻辑或：
  - `ori rt, rs, imm16`
- 加载存储：
  - `lw rt, imm16(rs)`
  - `sw rt, imm16(rs)`
- 条件分支：
  - `beq rs, rt, imm16`

以上指令中的立即数都是 16 位的，**运算时都需要扩展为 32 位**。其中 `ori` 为零扩展，其他均为符号扩展。

指令集的硬件需求：  
- **ALU**
  - 支持加法、减法、逻辑或、比较相等
  - 操作数：2 个 32 位数，来自寄存器或 32 位立即数
- **立即数扩展部件**
  - 将 16 位立即数扩展为 32 位
  - 符号扩展和零扩展
- **PC**
  - 一个 32 位寄存器
  - 两种加法：
    - PC + 4：取指令地址
    - PC + 4 + imm16 * 4：分支跳转地址
- **寄存器堆**
  - 32 个 32 位寄存器
  - 两个读端口，一个写端口
- **存储器**
  - 只读的指令存储器（32 位地址，32 位数据）
  - 读写的数据存储器（32 位地址，32 位数据）

寄存器堆：  
- 32 个 32 位寄存器
- 数据接口信号：
  - busA, busB：32 位读端口
  - busW：32 位写端口
- 读写控制：
  - Ra, Rb：5 位寄存器编号，分别对应 busA 和 busB
  - Rw：5 位寄存器编号，对应 busW
  - WriteEnable：写使能信号，在时钟信号 clk 的上升沿，若 WriteEnable == 1，则将 busW 写入对应寄存器
- 注意：寄存器堆的读操作不受时钟控制

存储器：  
- 数据接口信号：
  - Data In：32 位数据输入
  - Data Out：32 位数据输出
- 读写控制
  - Address：32 位地址
  - WriteEnable：写使能信号，在时钟信号 clk 的上升沿，若 WriteEnable == 1，则将 Data In 写入 Address 指向的存储单元
- 一个读端口，一个写端口

## 数据通路

取指单元（Instruction Fetch Unit, IFU）：  
- 组成部件
  - PC：程序计数器
  - 指令存储器
  - 两个加法器
  - 多选器：选择 PC + 4 或分支跳转地址
  - 与门：跳转当且仅当 `nPC_sel == 1` 且 `zero == 1`
  - 立即数符号扩展并左移部件
- 输入：
  - 时钟信号 clk 
  - 分支跳转信号 nPC_sel
  - ALU 的比较结果信号 zero
- 输出：
  - 指令

无论跳转指令 `beq rs, rt, imm16` 的条件是否成立，PC 都会加 4。如果成立，则 PC 会被继续加上 `SignExt[imm16] * 4`。

译码、执行、访存、写回单元：  
- 问题
  - 指令的目的寄存器（要写的寄存器）有时是 rd，有时是 rt
  - ALU 的第二个输入有时是寄存器堆的输出，有时是扩展后的立即数
  - 寄存器的 busW 有时是 ALU 的输出，有时是数据存储器的输出
- 组成部件
  - 寄存器堆
  - ALU
  - 立即数扩展部件
  - 多选器：从 rd 和 rt 中选择寄存器堆 busW
  - 多选器：从立即数扩展部件输出和寄存器堆 busB 中选择 ALU 的第二个输入
  - 数据存储器
  - 多选器：从 ALU 输出和数据存储器输出中选择寄存器堆 busW
- 输入：
  - 时钟信号 clk
  - 立即数
  - 控制信号
    - n_PC_sel："+4" 或 "branch"
    - ALUCtr："ADD"、"SUB"、"OR"
    - ALUSrc：ALU 的第二个输入的选择信号，"0" 为寄存器堆 busB，"1" 为立即数扩展部件的输出
    - RegWr：寄存器堆写使能信号
    - RegDst：寄存器堆写端口选择信号，"0" 为 rt，"1" 为 rd
    - ExtOp：立即数扩展的选择信号，"0" 为零扩展，"1" 为符号扩展
    - MemtoReg：寄存器堆 busW 选择信号，"0" 为 ALU 的输出，"1" 为数据存储器的输出
    - MemWr：数据存储器写使能信号（是否将 busB 写入数据存储器）

## 控制信号

R 型运算指令的控制信号：以 `addu rd, rs, rt` 为例：  
- nPC_sel："+4"
- ALUSrc："0"，ALU 的第二个输入来自寄存器堆 busB
- ExtOp: 任意
- ALUCtr："ADD"
- MemWr: "0"
- MemtoReg: "0"，寄存器堆 busW 选择 ALU 的输出
- RegWr: "1"
- RegDst: "1"，寄存器堆写端口选择寄存器 rd

I 型运算指令的控制信号：以 `ori rt, rs, imm16` 为例：  
- nPC_sel："+4"
- ALUSrc："1"，ALU 的第二个输入来自立即数扩展部件
- ExtOp: "0"，立即数扩展为零扩展
- ALUCtr："OR"
- MemWr: "0"
- MemtoReg: "0"，寄存器堆 busW 选择 ALU 的输出
- RegWr: "1"
- RegDst: "0"，寄存器堆写端口选择寄存器 rt

加载指令的控制信号：以 `lw rt, imm16(rs)` 为例：  
- nPC_sel："+4"
- ALUSrc："1"，ALU 的第二个输入来自立即数扩展部件
- ExtOp: "1"，立即数扩展为符号扩展
- ALUCtr："ADD"
- MemWr: "0"
- MemtoReg: "1"，寄存器堆 busW 选择数据存储器的输出
- RegWr: "1"
- RegDst: "0"，寄存器堆写端口选择寄存器 rt

跳转指令的控制信号：以 `beq rs, rt, imm16` 为例：  
- nPC_sel："branch"
- ALUSrc："0"，ALU 的第二个输入来自寄存器堆 busB
- ExtOp: 任意
- ALUCtr："SUB"
- MemWr: "0"
- MemtoReg: "x"
- RegWr: "0"
- RegDst: "x"

## 控制信号的集成

指令类型的控制信号可以由 opcode 和 func 只经**逻辑与**得到：  
- rtype = ~op5 & ~op4 & ~op3 & ~op2 & ~op1 & ~op0
  - 即 op 的 6 位都为 0
- add = rtype & func5 & ~func4 & ~func3 & ~func2 & ~func1 & ~func0
  - 即指令为 R 型且 func 为 100000
- sub = rtype & func5 & ~func4 & ~func3 & func2 & func1 & ~func0
  - 即指令为 R 型且 func 为 100010
- 其余指令同理

控制 CPU 执行的 8 个控制信号可以由指令类型的控制信号只经**逻辑或**得到：  
- nPC_sel = beq
- ALUSrc = ori | lw | sw
- ExtOp = lw | sw
- ALUCtr[0] = sub | beq
- ALUCtr[1] = ori
- MemWr = sw
- MemtoReg = lw
- RegWr = add | sub | ori | lw
- RegDst = add | sub

其中 ALUctr 有两位：  
- 00：加法
- 01：减法
- 10：逻辑或

# 控制器的基本原理

控制器接受指令的二进制编码，给出控制信号。

简单的指令集示例：  
- 运算类：`ADD R, M`
- 传送类：`LOAD R, M`、`STORE M, R`
- 转移类：`JMP L`
- 格式：4 位操作码 + 4 位寄存器号 + 8 位地址

模型机的组成：  
- 控制电路、指令译码电路
- IR
- PC
- MAR
- MDR
- 存储器

控制信号：  
- $IR_{IN}$：是否写入 IR
- $IR_{OUT}$：是否从 IR 读出操作码
- $PC_{OUT}$：是否从 PC 读出地址
- $PC_{加}$：是否将 PC 自增
- $MAR_{IN}$：是否写入 MAR
- $MDR_{OUT}$：是否从 MDR 读出数据
- $R1_{IN}$：是否写入 R1
- $M_{读}$：是否从存储器读出数据

`LOAD R1, M1` 指令的执行过程：  
1. 取指：
   1. 控制器发 $PC_{OUT}$ 和 $MAR_{IN}$ 信号，将 PC 的值送入 MAR
   2. 控制器发 $M_{读}$ 信号，从存储器中读出指令，送入 MDR
   3. 控制器发 $PC_{加}$ 信号，将 PC 加上指令长度
   4. 控制器发 $MDR_{OUT}$ 和 $IR_{IN}$ 信号，将 MDR 的值送入 IR
2. 执行：
   1. 控制器发 $IR_{OUT}$ 和 $MAR_{IN}$ 信号，将 IR 的地址部分送入 MAR
   2. 控制器发 $M_{读}$ 信号，从存储器中读出数据，送入 MDR
   3. 控制器发 $MDR_{OUT}$ 和 $R1_{IN}$ 信号，将 MDR 的值送入 R1

## 硬布线控制器

MIPS、PowerPC、ARM 采用硬布线控制器；奔腾采用硬布线 + 微程序控制器。

硬布线控制器又叫硬连线控制器或组合逻辑控制器，主要部件有：  
- 环形脉冲发生器：产生节拍脉冲信号（哪个时钟周期）
- 指令译码器（哪个指令）
- 微命令编码器：在不同节拍脉冲信号的同步下产生微命令信号（控制信号）

不同指令在不同机器周期应发出的控制信号：

| 指令  | T1  | T2 | T3 | T4 | T5 | T6 | T7 |
| :---: |:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| LOAD (00) | $PC_{OUT}, MAR_{IN}, M_{读}, PC_{加}$ | $MDR_{OUT}, IR_{IN}$ | $IR_{OUT}, MAR_{IN}, M_{读}$ | $MDR_{OUT}, R1_{IN}$ | End |  |  |
| ADD (01) | 同上 | 同上 | $IR_{OUT}, MAR_{IN}, M_{读}$ | $MDR_{OUT}, Y{IN}$ | $R1_{OUT}, add$ | $Z_{OUT}, R1_{IN}$ | End |
| STORE (10) | 同上 | 同上 | $IR_{OUT}, MAR_{IN}$ | $R1_{OUT}, MDR_{IN}, M_{写}$ | End |  |  |
| JMP (11) | 同上 | 同上 | $IR_{OUT}, PC_{IN}$ | End |  |  |  |

于是，控制信号 $MAR_{IN}$ 可以表示为：

$$
\begin{align*}
MAR_{IN} &= T1 \cdot (LOAD + ADD + STORE + JMP) + T3 \cdot (LOAD + ADD + STORE) \\
&= T1 + T3 \cdot (LOAD + ADD + STORE) \\
\end{align*}
$$

据此可以作出微命令编码器的逻辑电路。

硬布线控制器：  
- 优点：指令执行速度快
- 缺点：
  - 控制逻辑复杂
  - 设计、验证困难
  - 扩充、修改困难

## 微程序控制器

机器指令的执行过程可以分为多个微操作（micro-operation），每个微操作对应一个微指令（micro-instruction）。微指令存储在 CPU 内一个只读存储器中。

模型机有 16 个控制信号，微指令的微操作字段有 16 位，每一位都是一个控制信号，下址字段有 5 位，表示下一条微指令的微地址。若干微指令就组成了一个微程序。

微指令：  
- 微操作字段：16 位，每一位就是一个控制信号
- 下址字段：5 位
  - 最高位：是否分支（取指用的微指令的最后一条微指令需要，其下址字段需要根据译码得到的机器指令填充）
  - 低四位：下一条微指令地址

在开始执行机器指令时，先从控制存储器中读出**取指用的微指令**（微程序的公用部分），然后根据机器指令的操作码，找到对应的微程序入口地址，开始执行微程序，从而完成机器指令的执行。

微程序控制器：  
- 优点：
  - 规整：用程序来产生和组织微命令信号，用存储逻辑代替组合逻辑
  - 灵活：可扩展、可修改
- 缺点：
  - 速度慢

# 流水线处理器

五阶段：取指、译码、执行、访存、写回。

因为流水线寄存器的存在，流水线没有缩短单条指令的延迟，而是提高了吞吐量。

流水线可能不平衡：如果不同计算阶段的延迟不相等，则时钟周期不得不被设置为最长的延迟，以保证所有阶段都能完成。  
- 注意：不平衡的流水线的延迟是**最长阶段延迟乘以流水线阶段数**，而不是**所有阶段延迟之和**
- 将长延迟的阶段拆分成两个短延迟的阶段
- 随着流水线越来越深，流水线寄存器的延迟的影响会越来越大，且越来越复杂

有一个五级流水线的处理器设计，每一级的延迟分别为：450 ps，950 ps，450 ps，450 ps，450 ps。其中包含流水线寄存器延迟 50 ps。如果将第二级（950 ps）均匀拆分为两级，从而形成六级流水线。那在新处理器上，一条指令的执行时间是：3000 ps。

超标量（Superscaler）处理器：有两条或以上并行的流水线的处理器。

从单周期到流水线，是时间并行性的提升；从单流水线到超标量，是空间并行性的提升。

第一台超级计算机 CDC6600 已经有了超标量的雏形。

## 流水线冒险

流水线的三种冒险：  
- 结构冒险
- 数据冒险
- 控制冒险

结构冒险：由于**硬件资源不足**，流水线的某个阶段不能同时处理多条指令。  
- 如果指令和数据在同一存储器中，则不能同时访存和取指
  - 解决方法：bubble 或指令存储器与数据存储器分离
- 写回阶段和译码阶段都需要访问寄存器堆
  - 解决方法：寄存器读写速度很快，前半个时钟周期写，后半个时钟周期读

数据冒险：由于**数据依赖**，流水线的某个阶段不能同时处理多条指令。  
- `sub $t0, $s1, $s2; add $s3, $t0, $s4`，第二条指令需要等待第一条指令的结果
  - 正在译码的指令要读的寄存器还没有被先前的指令写回
  - 但译码并不真正用到读出的寄存器数据，所以更确切的说法是
  - **即将在执行阶段用到的寄存器数据还没有被先前的指令写回**
  - 解决方法：
    - bubble 两次（相当于在两条指令之间插入两个 `nop`）
    - 数据前递：**将访存阶段的 ALU 输出转发到执行阶段的 ALU 输入**，避免了 bubble
- `sub $t0, $s1, $s2; add $s3, $t0, $s4; and $s5, $s4, $t0`
  - 解决方法：
    - **将写回阶段的 ALU 输出转发到执行阶段的 ALU 输入**
- load-use 冒险：`lw $t1, 20($t0); or $t2, $t1, $s5`
  - **`or` 在执行阶段需要的操作数需要在 `lw` 的访存阶段完成后才能得到**
  - 解决方法：
    - bubble 一次（假设已经实现了数据前递）

控制冒险：由于**分支指令**，流水线的某个阶段不能同时处理多条指令。  
- `j Target`：
  - 目标地址在分支指令的取指阶段就可以获得，不需要 bubble
- `jr rs`：
  - 目标地址在分支指令的译码阶段才能获得，需要 bubble 一次
- `beq rs, rt, imm16`：
  - 我们可以在译码阶段增加逻辑，判断 `rs` 和 `rt` 是否相等
  - 目标地址在分支指令的译码阶段才能获得，需要 bubble 一次
- 延迟转移技术：**规定分支指令后面的一条指令一定会被执行**，即使分支不成立。

# 存储层次结构

冯诺依曼架构中的运算器、存储器以及外部记录介质都含有存储部件。

存储器特性：非易失性、可读可写、随机访问、访问时间、容量、价格、功耗

> 如果有一个非易失、可读可写、随机访问、访问时间短、容量大、价格低、功耗低的存储器，那就可以整个计算机系统只用它，而不需要存储层次结构了。

## DRAM

一个**内存模组**（内存条）上有若干 **DRAM 芯片**：  
- 每个 DRAM 芯片上有若干 **存储单元**（超单元），由行地址和列地址访问，一个超单元可存储 4 或 8 个比特
- 存储单元由电容和一个晶体管组成，电容会漏电，因此每隔一段时间需要刷新
- SDRAM: 同步 DRAM。DDR SDRAM 是它的升级

**取出地址 $A$ 中的一个字**：  
- 流程：
  - CPU 通过系统总线将地址 $A$ 发送到内存控制器
  - 内存控制器将 $A$ 转化成行列地址 $(i, j)$，发送到 $A$ 所在的内存模组
  - （对 DRAM 芯片**预充电**）
  - **ACT**（ACTivate）或 **RAS**（Row Access Strobe）：内存控制器将 $i$ 广播给其所有 DRAM 芯片。DRAM 芯片的行译码器将 $i$ 译码，选中对应的行，将行数据经放大放在行缓冲区中。**此过程需要 tRCD（Row to Column Delay）长的时间**
  - **CAS**：内存控制器将 $j$ 广播给其所有 DRAM 芯片。DRAM 芯片从行缓冲区中取出 $j$ 列的数据并输出。**此过程需要 CL（CAS Latency）个时钟周期**
  - 内存控制器采样数据，经过系统总线送入 CPU
- CAS 可以读多个数，每个时钟周期都能输出一个数 
- 如果下一次访问的数据在同一行，则可以省略 RAS，减少 tRCD 的延迟
- 否则预充电，将这一行关闭
- 预充电既可以在选中新行时进行，也可以在一次传输只剩最后一个字时进行（可以和最后一个字的传输重叠）
- 性能参数：
  - **tRCD**（Row to Column Delay）：从 RAS 到 CAS 的**时间**，约 2~3 个时钟周期（15~23ns）
  - **CL**（CAS Latency）：从 CAS 到数据输出的**时钟周期数**，约 2~3 个时钟周期
  - **tRP**（Row Precharge）：从预充电到 RAS 的**时间**，约 2~3 个时钟周期
- 两次行地址不同的 SDRAM 读需要间隔 tRCD/时钟周期 + CL + 数据个数 - 1 + tRP/时钟周期 个时钟周期

SDR 只在时钟上升沿传输和采样数据，DDR（Double Date Rate）在时钟上升沿和下降沿都传输和采样数据。

以送出四个数据为例：  
- SDR 需要 tRCD + CL + 4 个时钟周期
- DDR 需要 tRCD' + CL' + 2 个时钟周期
- DDR 厂商宣传的是 2 个数据每时钟周期的访存带宽，但由于时钟周期相比 SDR 变长，访存延迟（读出第一个数据的延迟）没有优势
- 不过后来工艺不断改进，SDR 就被淘汰了

DDR2-400 核心频率仍为 100MHz，但每次从核心取出 4 个数据，I/O 频率提升到了 200MHz，等效频率为 400MHz（每个 I/O 周期传输 2 个数据，200MHz * 2 = 400MHz）。

$$等效频率 = I/O频率 \times 2$$

$$I/O 频率 = 核心频率 \times 数据预取个数 / 2$$

$$带宽 = 等效频率 \times 64bit$$

每次 DDR 的换代基本都伴随着行选、列选延迟的提高，这一提高在代内的升级过程中又被逐渐改进。多年来，此延迟的提升非常有限，这主要被超单元中电容充放电的物理特性约束。

## SRAM

SRAM：  
- SRAM 的一个存储单元由 6 个晶体管组成，是一个**双稳态触发器**。它只能存储一个比特
- $BL$ 和 $\bar{BL}$ 传送读写数据信息，$WL$ 控制此存储单元是否可访问
- $VDD$ 是电源电压，$GND$ 是地线
- 写入 $1$ 时：
  - $BL = 1, \bar{BL} = 0, WL = 1$
  - 当晶体管状态稳定后，令 $WL=0$，则晶体管 $M5, M6$ 关闭，$BL$ 和 $\bar{BL}$ 信号撤销
  - 此时 SRAM 内部仍然能保持刚刚的状态，写入的这个 $1$ 被保存在 $Q$ 点。只要 $WL=0$，就能一直保持这个状态（当然，前提是通电）
- 读取 $1$ 时：
  - $WL = 1$，则 $BL$ 和 $\bar{BL}$ 就被 SRAM 自然置为 $1$ 和 $0$
- 组成：
  - 地址线、数据线、读写控制线、片选线
  - 译码驱动电路、读写控制电路
  - 存储矩阵

相比 DRAM，SRAM 的速度快、功耗高、集成度低（每位晶体管数多）、价格高、不用刷新

## Cache

时间局部性：最近访问的指令或数据很快还会被访问

空间局部性：访问的指令或数据附近的指令或数据也很快会被访问

循环有很好的时间局部性和空间局部性。数组访问有很好的空间局部性。

Cache 会一次取回一整块数据（空间局部性），并将其暂时存储在 Cache 中（时间局部性）。

内存地址被分割为三部分：  
- 标记位
- 组索引
- 块偏移
  
Cahe 的每个表项包含：  
- 有效位
- 标记位
- 数据块

Cache 先根据组索引找到对应的组，再检查组中每一行（本课示例中为直接映射高速缓存，每组仅一行）的标记位与有效位，如果匹配成功，则缓存命中，否则向主存请求数据，填入 Cache 中。

Cache 的写策略：  
- 缓存命中：
  - 写穿透：将数据写入 Cache 和主存
  - 写返回：懒惰写入，只将数据写入 Cache，直到数据被替换时才写入主存（利用时间局部性，多次写入可以只写最后一次）
- 缓存未命中：
  - 写不分配：直接将数据写入主存
  - 写分配：将数据读入 Cache，再写入 Cache（利用时间局部性，写入后可能被再次访问）
- 写返回和写分配一般结合使用，适合复杂高效的系统
- 写穿透和写不分配一般结合使用，适合简单的系统

Cache 的平均访问时间：

$$Average Memory Access Time = Hit Time + Miss Rate \times Miss Penalty$$

Cache 的失效原因：  
- 义务失效（Compulsory Miss）：冷访问，Cache 中没有数据
  - 无法避免
- 容量失效（Capacity Miss）：Cache 容量不足，数据被替换
  - 可以通过增大 Cache 容量缓解
- 冲突失效（Conflict Miss）：Cache 中有足够的空间，但多个数据映射到同一行
  - 可以通过增加组相联路数缓解

组相联路数越多，cache 越复杂，命中时间越长。

Cache 替换算法：随机、round-robin、LRU（Least Recently Used）

Cache 实例：  
- 80486 指令和数据共用 cache，指令和数据的局部性互相影响
- Pentium 指令和数据分开 cache
- Core I7 多级 cache

## 单位

除了计算机内部的存储器（cache、内存）以 $1024$ 为底，其他单位都以 $1000$ 为底。  
- 计算机内部的存储器（cache、内存）的单位以 $1024$ 为底
- 外部存储器（硬盘、优盘）的单位以 $1000$ 为底
- **网速** Mbps、**时钟频率** GHz、SATA **带宽** Gbit/s 以 $1000$ 为底
- Ki、Mi、Gi 前缀专门表示以 $1024$ 为底的单位


# 中断和异常

第一个带异常处理的计算机：UNIVAC。一旦算术溢出，转向地址 $0$ 执行两条修复指令，或停机

第一个带外部中断的计算机：DYSEAC。有两个 PC，根据 I/O 信号切换。

中断（异常）的叫法种类很多：  
- 外部事件产生的：外部中断、硬件中断、中断
- 内部事件产生的：内部中断、软件中断、异常
- 二者统称中断，也可统称异常

8086 的存储器中除了存放一般数据和指令的区域外，还保留了两个专用区域：  
- **中断向量表区**：00000H~003FFH，低 1KB 地址
  - 256 个中断向量（中断服务程序的入口地址），每个中断向量 4 字节
  - 中断向量：**前两个字节存偏移量，后两个字节存段基址。小端法（低字节在前）**
- 初始化程序区：FFFF0H~FFFFFH，高 16B 地址
  - CPU 复位后从 FFFF0H 取出第一条指令，通常是到系统程序入口的无条件转移
- 256 个中断中，有 5 个是专用的，27 个是保留的，其余 224 个都由用户定义
- 如果中断类型码是 17H，中断服务程序入口地址是 2340H:7890H，则中断向量存放在 5CH，中断向量内容为 [90H, 78H, 40H, 23H]。

保护模式下，虽然逻辑地址仍然写成段基址:偏移量的形式，但物理地址的产生方式不同了。（参考 IA-32 的寻址方式）  
- 保护模式的中断向量表（中断描述符表 IDT）可以在内存任意地方，存放 256 个 8 字节的中断描述符
- IDT 的地址存放在 CPU 内部的 IDTR 寄存器中
- 中断描述符中包括 16 位的段选择符和 32 位的偏移地址，它们会被分别加载到 CS 和 EIP 寄存器中
- 中断描述符的地址 = 中断类型号 * 8 + IDTR 中的基址

中断处理过程：  
1. CPU 关闭中断响应（置零标志寄存器 FLAGS 中的 IF 位）
  - STI 指令和 CLI 指令
   - 对内部中断和非屏蔽中断无效
2. 压栈保存断点（可能是发生中断的指令的地址，也可能是其下一条指令的地址）
3. 确定中断类型号
4. 保存现场（有关寄存器和标志寄存器）
5. 跳转到中断服务程序开始执行（可以适当时刻开放中断响应，以免过长时间关闭中断）
6. 恢复现场并返回
  - IRET 指令：弹栈 3 个字，分别放入 IP、CS、FLAGS 寄存器中（后来又有针对 32 位、64 位的 IRETD、IRETQ）
- 通常前三步由硬件完成，后三步由软件（中断服务程序）完成

## 内部中断

内部中断：由 CPU 内部产生的中断  
- **除法错中断**（0）：商超出目标寄存器范围，如除以零
- **溢出中断**（4）：执行 INTO 指令（等价于 INT 4）时，若 OF 为 1，则引起溢出中断
- **单步中断**（1）：当 TF 为 1 时，CPU 每执行一条指令就自动引起一个单步中断。单步中断服务程序一般会显示各寄存器内容等信息，用于调试
  - 单步中断可以通过软件方法屏蔽
- **断点中断**（3）：
  - INT 3 指令是唯一单字节长的 INT 指令，其他 INT 指令都是双字节长的（0~256 的中断类型码就需要 8 位）
  - 使用步骤：
    - 用 INT 3 替换用户程序欲断点位置的原有指令，保存此原有指令
    - 用户程序运行到此处时便会执行 INT 3，进入断点中断服务程序
    - 断点中断服务程序返回前，恢复原有指令，并将 IP 减 1
    - 返回后，用户程序会从断点处继续执行原有指令
  - 单字节长保证了 INT 3 不会替换两个用户指令，导致控制流被扰乱

内部中断的特点：  
- 中断类型号都在 CPU 内部产生（外部中断需要从外部读取）
- 除单步中断可以通过软件设置 TF 控制外，所有内部中断都不能用软件方法屏蔽
- 除单步中断外，所有内部中断的优先级都比外部中断高

INT n 指令：  
1. 压栈 FLAGS
2. 清除中断标志 IF 和单步标志 TF
3. 压栈 CS 和 IP
4. 根据中断类型码 n 查中断向量表，取出中断服务程序的段基址和偏移量
5. 将 CS 和 IP 寄存器分别装入中断服务程序的段基址和偏移量

BIOS 存放在 ROM 中，提供系统加电自检、引导、主要 I/O 设备的处理程序等功能。

BIOS 各功能模块的入口地址存放在中断向量表，可由 INT n 指令调用（可通过寄存器传递参数）

DOS 中断：INT 21H  
- 包含常用功能程序，实现文件管理、存储管理等
- 通过传入功能号参数选择功能
- 比 BIOS 中断功能齐全
- 进一步屏蔽了设备的物理特性和接口特性

## 外部中断

x86 CPU 为外部中断提供两个引脚  
- NMI：非屏蔽中断
- INTR：可屏蔽中断

NMI 引脚直接连接外设，INTR 引脚连接中断控制器，中断控制器连接外设。

可编程中断控制器（PIC）：8259A

现代计算机中，APIC（Advanced PIC）被集成到南桥，CPU 中还有用于多核通信的 Local APIC。

可屏蔽中断的处理过程：  
- 外设向 CPU 发送中断请求信号
- CPU 完成先行指令，发出中断响应信号
- CPU 读取中断类型码
- 之后与内部中断相同：
- 压栈当前 CS 和 IP
- 清除中断标志 IF 和单步标志 TF
- 取 CS 和 IP 的新值，跳转到中断服务程序
- 在中断服务程序中，可以适当开启中断
- 中断服务程序结束后，恢复现场

中断嵌套：在中断服务程序中，如果开启了中断，且 CPU 收到优先级更高的中断请求，则 CPU 会响应新的中断请求，完成后再返回到原来的中断服务程序。

# 输入输出接口

模型机的输入输出：I/O 端口统一编址

I/O 接口的动机：  
- CPU 和外设的速度差异
- 外设处理的信息格式、接口信号多样
  - CPU 的最小操作单元是字节，但串行设备是一位一位工作的
  - CPU 处理数字信号（方波），但喇叭是模拟信号（正弦波）
  - CPU 的电压和外设设备不同
  - 如果让 CPU 处理外设，CPU 的设计会变得极其复杂
- I/O 接口电路处理以上问题

I/O 接口的功能：  
- 数据缓冲
  - 解决 CPU 和外设的速度差异：少次多量处理
- 提供联络信息
  - 协调与同步数据交换过程
  - 网卡：网线通不通？有没有冲突？
- 信号与信息格式的转换
  - 模/数、数/模转换，串/并、并/串转换，电平转换
- 设备选择
- 中断管理
- 可编程功能

I/O 接口的组成：  
- 数据输入寄存器
- 数据输出寄存器
- 状态寄存器
- 控制寄存器
- 中断控制逻辑

现代 PC 中的 I/O 接口：  
- 网卡
- 并口控制芯片
- 南桥
- 显卡

## I/O 端口及其编址

**I/O 端口**：I/O 接口内部的一组寄存器。每个 I/O 端口有自己的端口地址（端口号），供 CPU 访问

I/O 端口编址方式：  
- I/O 端口和存储器分开编址
  - I/O mapped I/O
  - x86
- I/O 端口和存储器统一编址
  - Memory mapped I/O
  - ARM、模型机、MIPS

x86 的 I/O 指令：  
- `IN AC, PORT`：从 I/O 端口读入数据到 AL 或 AX
- `OUT PORT, AC`：将 AL 或 AX 的数据输出到 I/O 端口
- 端口地址 PORT 范围为 0~255，更大的地址用**间接寻址**
  - `IN AC`：从 DX 指向的端口读入数据到 AL 或 AX
- x86 系统总线中有一个 M/IO 信号，指明是存储器访问还是 I/O 端口访问

分开编址：  
- 优点
  - I/O 端口不占用存储器地址
  - I/O 专用指令的编码更短（尤其地址码），指令执行更快
  - 程序可读性更强

统一编址：  
- 优点
  - 用功能齐全的 MOV 指令访问 I/O 端口
  - I/O 访问与存储器访问统一为一套控制逻辑，简化内部结构，减少 CPU 的引脚数目
- 缺点
  - 由于I/O端口占用了一部分存储器地址空间，因而使存储地址空间减小
  - 由于利用访问存储器的指令来进行I/O操作，指令的长度通常比单独 I/O 指令要长，因而指令的执行时间也较长

## I/O 控制方式

I/O 控制方式的分类：  
- 程序控制方式
- 中断控制方式
- 直接存储器访问（DMA）控制方式

### 程序控制方式

程序控制方式：  
- 无条件传送方式
  - 直接传输数据，不检查外设是否准备好等信息
  - 优点：控制程序简单
  - 缺点：只适合简单外设
  - LED 灯
- 程序查询传送方式
  - CPU 通过执行一段程序不断查询外设状态
  - 当外设就绪时，才进行数据传送
  - 优点：准确可靠
  - 缺点：查询外设状态占用大量 CPU 时间
- 两种程序控制方式共同的优点：
  - 对外设要求低
  - 操作流程清晰
- 共同的缺点：
  - CPU 负责数据传送，占用 CPU 资源

程序查询方式的数据输出：  
- CPU 执行 OUT 指令，将**控制字**写入接口的**控制寄存器**，设置其工作模式
- CPU 执行 OUT 指令，将数据写到接口的**输出缓冲寄存器**中
- 接口将数据发到**并行数据输出信号线**，将**输出准备好信号**置为有效
  - 亦可由 CPU 写控制字将输出准备好信号置为有效
- 外设一旦发现**输出准备好信号**有效，就从**并行数据输出信号线**读取数据，将**输出回答信号**置为有效
- 接口发现**输出回答信号**有效，就将**状态寄存器**中的**输出缓冲空状态位**置为有效
- 此过程中，CPU 反复从状态寄存器中读出**状态字**，并检查状态字中的**输出缓冲空位**是否有效，若有效，则可以开启下一个输出过程


程序查询方式的数据输入：  
- 系统初始化时，CPU 将**控制字**写入接口的**控制寄存器**，设置其工作模式
- 外设将数据发到**并行数据输入信号线**，将**输入准备好信号**置为有效
- 接口发现**输入准备好信号**有效后，从**并行数据输入信号线**接收数据，放入**输入缓冲寄存器**，并将**输入回答信号**置为有效，阻止外设继续发送数据
- 接口将**状态寄存器**中**输入缓冲满状态位**置为有效
- CPU 反复从状态寄存器中读出**状态字**，并检查状态字中的**输入缓冲满位**是否有效，若有效，则从输入缓冲寄存器中读出数据
- 接口将**输入回答信号**置为无效，允许外设继续发送数据

### 中断控制方式

中断控制方式的数据输入：  
- 外设和并行接口电路的交互和程序控制方式相同
- 系统初始化时，CPU 将**控制字**写入接口的**控制寄存器**，设置其工作模式
- 外设将数据发到**并行数据输入信号线**，将**输入准备好信号**置为有效
- 接口发现**输入准备好信号**有效后，从**并行数据输入信号线**接收数据，放入**输入缓冲寄存器**，并将**输入回答信号**置为有效，阻止外设继续发送数据
- 接口通过**中断控制逻辑**向 CPU 发出中断请求信号，并将**状态寄存器**中**输入缓冲满状态位**置为有效
- CPU 响应中断请求，执行中断服务程序：
  - 中断服务程序中，CPU 从状态寄存器中读出**状态字**，并检查状态字中的**输入缓冲满位**是否有效，若有效，则从输入缓冲寄存器中读出数据
- 接口将**输入回答信号**置为无效，允许外设继续发送数据


中断控制方式的数据输出：  
- CPU 执行 OUT 指令，将**控制字**写入接口的**控制寄存器**，设置其工作模式
- CPU 执行 OUT 指令，将数据写到接口的**输出缓冲寄存器**中
- 接口将数据发到**并行数据输出信号线**，将**输出准备好信号**置为有效
  - 亦可由 CPU 写控制字将输出准备好信号置为有效
- 外设一旦发现**输出准备好信号**有效，就从**并行数据输出信号线**读取数据，将**输出回答信号**置为有效
- 接口发现**输出回答信号**有效，通过**中断控制逻辑**向 CPU 发出中断请求信号，并将**状态寄存器**中的**输出缓冲空状态位**置为有效
- CPU 响应中断请求，执行中断服务程序：
  - 中断服务程序中，CPU 从状态寄存器中读出**状态字**，并检查状态字中的**输出缓冲空位**是否有效，若有效，则可以开启下一个输出过程

中断控制方式的特点：  
- 优点：
  - CPU 和外设并行工作
  - 外设拥有申请服务的主动权
  - 满足了外设的实时性要求
- 缺点：
  - 外设和存储器之间的数据传送仍然需要 CPU 参与，占用 CPU 资源
    - 数据经过 CPU 寄存器中转，过程冗长
  - 中断本身的上下文切换开销

### 直接存储器访问（DMA）

直接存储器访问（DMA）：  
- 数据传送不需要 CPU 参与
- 由专门硬件控制电路 **DMA 控制器**（**DMAC**）控制，外设和存储器直接交换数据
- 需要和程序控制方式或中断方式结合使用

DMAC 工作步骤：以外设到内存传送为例  
1. CPU 设置 DMAC 内部配置寄存器
   - 一般包括：
     - 源地址初始值及地址增减方式，例如设置为某 I/O 端口，传送时不变
     - 目的地址初始值及地址增减方式，例如设置为内存地址，传送时递增
     - 传送数据长度（根据需要设置）
2. DMAC 处于空闲等待状态
3. I/O 接口向 DMAC 发出 DMA 传送申请
4. DMAC 响应 I/O 接口的申请
5. DMAC 向 I/O 接口发起总线读传输
6. DMAC 向存储器发起总线写传输
7. DMAC 重复 5 和 6，直到传送完成
   - 通常此时 DMAC 会通过中断通知 CPU
8. 返回 2，等待下一个 DMA 传送请求

有的 I/O 接口自带 DMAC，如声卡、网卡、显卡


## 并行接口

并行接口电路：  
- 以前多是独立芯片，如 Intel 8255A
- 现在多包含在一块多功能芯片中
- 目前现在很少接出来接口了，被 USB 取代了
- 通过并行的 8 根数据线以及 2 根握手信号线与外设连接


串行与并行：  
- 串行传输线数量少，并行传输线数量多
- 系统总线是并行的，串行通信需要复杂的串/并转换，而并行通信不需要
- 同频率下，并行的数据传输率高，但串行频率更高
- 串行避免信号线的串扰，并行则需要考虑串扰问题

现在串行更多（USB、PATA -> SATA）：  
- 差分信号传输技术
  - 发送端在两根线上发振幅相同、相位相反的信号
  - 接收端比较两根线的电压差，得到原始信号
  - 干扰完全不会影响两根线的差值（两根线很近，受到的干扰相同，都上升 1V 差值不变）
  - 而且差分信号也不会干扰别人
  - 缺点：需要两根等长等宽紧密相邻的线，布线难度高

## 并口芯片应用实例

Intel 8255A 的外部接口：  
- 电源 Vcc 和地线 GND
- 与外设的连线：
  - 端口 A：$PA_7$~$PA_0$
  - 端口 B：$PB_7$~$PB_0$
  - 端口 C：$PC_7$~$PC_0$
- 与系统总线的连线：
  - 复位：RESET
  - 数据线：$D_7$~$D_0$
  - 地址线：$A_1$、$A_0$
  - 控制：CS、RD、WR

与外设的连线：  
- A 和 B 均为 8 位端口，可分别设定为输入端口或输出端口，端口 A 功能更丰富
- 端口 C 分为两个 4 位的端口，可分别设定为输入端口或输出端口
  - 通常传送 A 和 B 的握手信号

8255A 有 4 个端口（可编程寄存器），因此有 2 根地址线  
- 当 CS 有效时，根据两根地址线选中其中一个端口
- 00~11 对应端口 A、B、C、控制端口

8255A 有三种工作方式：  
- 方式 0：基本输入/输出方式
  - 单向输入/输出，无握手信号
- 方式 1：选通输入/输出方式
  - 单向输入/输出，端口 C 用于握手信号
- 方式 2：双向输入/输出方式
  - 双向输入/输出，端口 C 用于握手信号

8255A 初始化时，用 OUT 指令向控制端口发送控制字。

控制字：

- 最高位为 0：方式选择控制字
- 最高位为 1：端口 C 按位置 1/置 0 控制字

# 总线

## 总线的基本结构

总线（bus，汇流排）：在多个模块之间传送数据的公共通路，包括：  
- 传输信息的电路
- 管理信息传输的协议

计算机中的总线：  
- 片总线（器件级总线）-> 片上总线
- 内总线（系统总线）
- 通信总线

总线模块：  
- 总线主模块（bug master）
  - 有总线控制能力，获得总线控制权后可以主动发起总线传输
  - CPU、DMA 控制器
- 总线从模块（bus slave）
  - 无总线控制能力，不能主动发起总线传输，只能被动响应总线主模块的请求
  - 存储器

总线译码器（bus decoder）：  
- 根据当前控制总线的总线主模块发出的地址信号，选择本次传输目标的总线从模块
- 从模块可以无视译码器的选中信号，总是响应总线主模块的请求

总线仲裁器（bus arbiter）：  
- 当多个主模块同时请求总线控制权时，仲裁器决定哪个主模块获得总线控制权
- 仲裁策略：静态、round-robin

总线内部有若干多选器：  
- 接收仲裁器和各主模块的信号，根据仲裁器的控制信号选择其中一个主模块的信号，输出给各从模块。

总线设计理念：  
- 可以乱收多收
- 不准乱发：乱发也没用

为了同步更慢的设备，可以通过总线桥接器（bus bridge），再连接一条更慢更小的总线。

## 常见总线协议

